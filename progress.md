2026-07-06 追加更新：完成 LangGraph worktree 合回主 tree 前的合并候选整理与验证。范围仍限定在 `D:\tmp\student-planner-langgraph-final` 的 `codex/langgraph-rag-submission` worktree；已把主 tree 独有提交 `08d1cbd Align RAG chat response contract` 合入 LangGraph 分支并解决 `progress.md`、`student-planner/app/agent/loop.py` 冲突，保留 LangGraph 的 pending confirmation / confirmed write 状态链路，同时保留 master 的 RAG `answer_kind/grounding` 响应契约。合并后发现并修复两个阻塞点：1）OpenAI-compatible stub 可能返回 `content: None`，`study_planner.py` 现在把空内容按空字符串处理并走 fallback 计划；2）LangGraph confirmed write 路径里，确认卡已经包含的 `reminder_advance_minutes=30` 可能被后续模型二次 `create_task` 的 `null` 覆盖，`loop.py` 现在会在确认落库前从 pending confirmation data 恢复该提醒参数，避免“确认创建带提醒任务”最终只写 task 不写 reminder。E2E 里的 `lower daily limit` 失败不是状态管理回归，而是测试夹具写死 `2026-06-09/10`，当前日期已经是 `2026-07-06`，代码按“未来 30 天”查询自然找不到；已把该夹具改为相对当前日期未来 14/15 天。

验证结果：`git diff --check` 通过（仅 CRLF warning）；`scripts/langgraph_ws_smoke.py --db D:\tmp\student-planner-langgraph-merge-smoke-final.db --stub-port 18228` 通过，task/study/work/schedule/course/RAG/no-web/plain_chat DB invariant 均正常；后端分组矩阵使用独立 `--basetemp` 串行通过：第一组 `80 passed`，第二组 `50 passed`，第三组 `30 passed, 1 warning`（末尾仍有既有 Windows/aiosqlite access violation 噪声但退出码为 0）；前端 `npm.cmd test -- src/stores/chatStore.test.ts src/pages/ChatPage.test.tsx` -> `63 passed | 2 skipped`，`npm.cmd run typecheck` 通过，`npm.cmd run build` 通过；真实浏览器 `npm.cmd run e2e:agent-loop -- --reporter=list --global-timeout=900000` 现在 `12 passed (4.5m)`。本轮未修改 DB schema、RAG corpus、OCR、scheduler 或前端聊天协议；运行产物如 Chroma sqlite、E2E sqlite、test-results/dist 仍需在提交前排除。下一步：清理运行产物，提交 LangGraph 分支修复，然后检查 `D:\student_time_plan` 主 tree 的未提交状态；只有主 tree 可安全合并时才执行正常 `git merge`，不使用目录复制覆盖。

# Student Planner 当前进度

2026-06-21 追加更新 2：沿“scope/weak_areas 选填但按考试绑定”的方向修复 multi-exam 复习计划 live 失败。本轮继续只在 `D:\tmp\student-planner-langgraph-final` 的 `codex/langgraph-rag-submission` worktree 工作。确认后的真实失败点不是 scope 必填，而是模型在 `get_free_slots` 后没有真实调用 `create_study_plan`，却输出了普通 assistant 文本形式的 `[TOOL_SUMMARY:create_study_plan:*]` 和计划预览，导致没有 agent log、没有 review/write workflow、也没有 `create_task` 落库。实现：`create_study_plan.exams[]` 增加可选 `scope` / `weak_areas` schema，`study_planner.py` 在生成 prompt 与 fallback 前把 `study_context.raw_notes/exam_scope` 中能归属到具体课程的范围和薄弱项绑定到对应 exam；`loop.py` 新增 ISO 日期考试请求解析与确定性 study-plan 本地入口；`langgraph_loop.py` 新增 `run_langgraph_study_plan_workflow()`，完整请求现在走 `get_free_slots -> create_study_plan -> plan_review_write -> confirmed_write/create_task`，不再依赖模型下一轮自觉发结构化工具调用。验证：`$env:TMP='D:\tmp\pytest-tmp-native'; $env:TEMP='D:\tmp\pytest-tmp-native'; .\student-planner\.venv-native\Scripts\python.exe -m pytest -q student-planner/tests/test_study_planner.py student-planner/tests/test_tools_schema.py student-planner/tests/test_agent_loop_task_creation.py::test_agent_loop_writes_confirmed_study_plan_tasks student-planner/tests/test_agent_loop_task_creation.py::test_agent_loop_writes_multi_exam_study_plan_tasks student-planner/tests/test_langgraph_action_workflows.py::test_langgraph_native_study_plan_confirmed_write_does_not_delegate` -> `21 passed`；真实浏览器定向 `$env:AGENT_E2E_PYTHON='D:\tmp\student-planner-langgraph-final\student-planner\.venv-native\Scripts\python.exe'; $env:AGENT_E2E_DATABASE_URL='sqlite+aiosqlite:///./agent_loop_e2e_langgraph_multiexam_fix.db'; npm.cmd run e2e:agent-loop -- --grep "multi-exam" --reporter=list --global-timeout=420000` -> `1 passed (34.5s)`。当前仍未处理上一轮的 `lower daily limit`，它仍更像过期 E2E/日期窗口问题；本轮未修改 WebSocket/前端协议、DB schema、RAG corpus、scheduler、OCR 或前端代码；未 push。

2026-06-21 追加更新：复测 LangGraph worktree 真实浏览器 Agent Loop E2E，并修复相对主 tree 的两类明确回归。本轮继续只在 `D:\tmp\student-planner-langgraph-final` 的 `codex/langgraph-rag-submission` worktree 工作；开始时既有运行产物包括 Chroma 文件、logs、旧 `agent_tool_node_*.db` 和 E2E DB，未纳入源码改动。根因 1：`decide_agent_route()` 在判断 RAG answer candidate 时，`机器学习报告 + 拆成任务` 同时命中 RAG 语义和作业拆解语义，但 work-plan/study-plan 识别没有覆盖“报告/大作业/项目 + 拆成/拆解/分解”，导致作业报告拆解被 RAG insufficient gate 截断。实现：`contracts.py` 扩展 tool/study-plan markers，使“2026-07-03 要交机器学习报告，帮我拆成任务。”在 RAG 证据不足时仍保持 `AgentRoute.STUDY_PLAN / should_retrieve=True / should_gate_rag_answer=False / retrieval_mode=local_rag_context`。根因 2：LangGraph action route 复用旧 action loop 时，内部 `[TOOL_SUMMARY:*]` 文本事件可能作为普通 `text_delta/text` 转发给 UI。实现：`langgraph_loop.py` 在 action route 事件转发层过滤仅用户可见的内部 tool summary 文本事件，保留真实 tool_result/证据 JSON。新增回归：`tests/test_agent_contracts.py::test_router_contract_keeps_assignment_breakdown_on_work_plan_path`，`tests/test_langgraph_rag_runtime.py::test_prepare_langgraph_state_routes_assignment_breakdown_to_plan_not_rag_gate`，`tests/test_langgraph_rag_runtime.py::test_langgraph_agent_loop_does_not_emit_internal_tool_summary_text`。验证：`$env:TMP='D:\tmp\pytest-tmp-native'; $env:TEMP='D:\tmp\pytest-tmp-native'; .\.venv-native\Scripts\python.exe -m pytest -q tests/test_agent_contracts.py::test_router_contract_keeps_assignment_breakdown_on_work_plan_path tests/test_langgraph_rag_runtime.py::test_prepare_langgraph_state_routes_assignment_breakdown_to_plan_not_rag_gate tests/test_langgraph_rag_runtime.py::test_langgraph_agent_loop_does_not_emit_internal_tool_summary_text tests/test_langgraph_rag_runtime.py::test_prepare_langgraph_state_routes_study_plan_to_native_action_node tests/test_langgraph_rag_runtime.py::test_prepare_langgraph_state_routes_rag_insufficient_inside_graph` -> `5 passed`；真实浏览器定向回归 `$env:AGENT_E2E_PYTHON='D:\tmp\student-planner-langgraph-final\student-planner\.venv-native\Scripts\python.exe'; $env:AGENT_E2E_DATABASE_URL='sqlite+aiosqlite:///./agent_loop_e2e_langgraph_fix.db'; npm.cmd run e2e:agent-loop -- --grep "assignment report plan|reschedules a confirmed plan task|keeps one task stable" --reporter=list --global-timeout=600000` -> `3 passed (3.1m)`。UI 复核：作业报告拆解成功卡显示“已把 10 条作业任务写入日程”，冲突场景显示“其中 1 条因原时间冲突已自动重排”，重复任务场景不再把 `[TOOL_SUMMARY]` 渲染到聊天 UI；三张截图位于 `D:\tmp\student-planner-langgraph-final\output\playwright\agent-loop-e2e-*.png`。修复后全量真实浏览器 E2E `$env:AGENT_E2E_PYTHON='D:\tmp\student-planner-langgraph-final\student-planner\.venv-native\Scripts\python.exe'; $env:AGENT_E2E_DATABASE_URL='sqlite+aiosqlite:///./agent_loop_e2e_langgraph_full_after_fix.db'; npm.cmd run e2e:agent-loop -- --reporter=list --global-timeout=900000` -> `10 passed, 2 failed (12.2m)`；剩余失败为 `multi-exam` 与 `lower daily limit`。当前主 tree 复核 `$env:AGENT_E2E_BACKEND_PORT='8021'; $env:AGENT_E2E_FRONTEND_PORT='5188'; $env:AGENT_E2E_DATABASE_URL='sqlite+aiosqlite:///./agent_loop_e2e_main_remaining_compare.db'; npm.cmd run e2e:agent-loop -- --grep "multi-exam|lower daily" --reporter=list --global-timeout=600000` -> `2 failed`，同样两条 240s timeout；已写入 `bugs.md`，先按既有尾部质量问题而非 LangGraph worktree 专属回归处理。本轮未修改 WebSocket/前端协议、DB schema、RAG corpus、scheduler、OCR 或前端代码；未 push。

2026-06-20 追加更新：移除 LangGraph runtime 的 legacy Agent Loop fallback。本轮继续只在 `D:\tmp\student-planner-langgraph-final` 的 `codex/langgraph-rag-submission` worktree 工作；开始时分支为 `## codex/langgraph-rag-submission...origin/codex/langgraph-rag-submission [ahead 14]`，既有 Chroma、logs、旧 smoke DB 运行产物仍未纳入提交。实现：`langgraph_loop.py` 删除对旧 `run_agent_loop()` 的导入和最终 `else` fallback，移除 `should_delegate_legacy_loop` 状态字段和 `delegate_legacy_loop` 图节点，新增 `unsupported_route` graph terminal；未知/未覆盖 route 现在直接返回“当前 LangGraph 运行时没有可用的处理路径，请换一种说法或明确任务类型。”，不会进入普通 LLM 自由回答。`chat.py` 的 `_select_agent_loop()` 固定返回 `run_langgraph_agent_loop()`，`app/config.py` 默认 `agent_runtime` 改为 `langgraph`，因此 WebSocket 入口不再可配置回旧 loop；旧 `app.agent.loop.run_agent_loop()` 仍保留给直接后端回归和 helper 兼容，不再是 LangGraph/chat fallback。同步更新 `LANGGRAPH_MIGRATION_CONTRACTS.md`、`tests/test_langgraph_rag_runtime.py`、`tests/test_chat_ws.py`、`tests/test_langgraph_action_workflows.py`，新增 unsupported route 回归和“LangGraph module 不暴露 run_agent_loop”断言；旧 patch 陷阱改为 `create=True`，确保未来重新接入旧 loop 会失败。验证：`.venv-native` 本地 `pyvenv.cfg` 原先指向已不存在的 `C:\Users\Chen\AppData\Local\Programs\Python\Python312\python.exe`，本轮仅在未提交 venv 内临时指回 `C:\Users\Chen\anaconda3\python.exe` 以便执行用户指定 native 命令；并行跑测试会抢同一个硬编码 `./test.db`，因此最终均串行执行。`.\.venv-native\Scripts\python.exe -m py_compile app\agent\langgraph_loop.py app\routers\chat.py app\config.py tests\test_langgraph_rag_runtime.py tests\test_chat_ws.py tests\test_langgraph_action_workflows.py` 通过；定向 `$env:TMP='D:\tmp\pytest-tmp-native'; $env:TEMP='D:\tmp\pytest-tmp-native'; .\.venv-native\Scripts\python.exe -m pytest -q tests/test_langgraph_rag_runtime.py -k "router_shell or unsupported_route or selector or no_web or rag_events or plain_chat or evidence or arrangement or ask_user"` -> `15 passed, 25 deselected`；`tests/test_chat_ws.py` -> `7 passed, 1 warning`；`tests/test_langgraph_action_workflows.py` -> `11 passed`；完整后端矩阵 `$env:TMP='D:\tmp\pytest-tmp-native'; $env:TEMP='D:\tmp\pytest-tmp-native'; .\.venv-native\Scripts\python.exe -m pytest -q tests/test_agent_contracts.py tests/test_langgraph_rag_runtime.py tests/test_langgraph_tool_node.py tests/test_langgraph_action_workflows.py tests/test_agent_db_write_gate.py tests/test_agent_loop.py tests/test_agent_loop_task_creation.py tests/test_agent_loop_schedule_import_shortcut.py tests/test_chat_ws.py tests/test_tool_preflight.py tests/test_tools_schema.py` -> `148 passed, 3 warnings in 162.24s`，warnings 为既有 Starlette/TestClient deprecation、APScheduler coroutine cleanup、SQLAlchemy pooled connection cleanup；pytest 结束后仍有 Windows/aiosqlite access-violation 噪声但命令 exit code 为 0。真实 WS smoke `.\.venv-native\Scripts\python.exe scripts\langgraph_ws_smoke.py --db D:\tmp\student-planner-langgraph-no-fallback-smoke.db --stub-port 18216` 通过，脚本显式启动 `127.0.0.1:8001` 且设置 `SP_AGENT_RUNTIME=langgraph`；RAG hit graph_nodes = `route -> retrieve_rag -> compose_runtime_hints -> rag_qa`，RAG insufficient graph_nodes = `route -> retrieve_rag -> rag_insufficient`，plain chat 为 `text_delta -> text -> done`，task/schedule/study/work/course 路径保持写库 invariant，所有可见 graph_nodes 均不含 `delegate_legacy_loop`。本轮未修改 WebSocket/前端协议、DB schema、RAG corpus、Chroma 数据库内容、scheduler、OCR 或前端代码，因此未跑 frontend test/typecheck；未 push。

2026-06-20 追加更新：修复 LangGraph work-plan workflow 中 3 处用户可见乱码文案。本轮继续只在 `D:\tmp\student-planner-langgraph-final` 的 `codex/langgraph-rag-submission` worktree 工作；根因是前一轮迁移新增的 work-plan fallback/cancel 分支里保留了 mojibake 字符串，主 happy path 与 smoke 未覆盖这些边缘分支。实现：替换 `langgraph_loop.py` 中 review override 删除全部任务、缺少作业截止日期、用户取消补充作业上下文三处文案；新增 `tests/test_langgraph_action_workflows.py` 覆盖这三条分支，并断言输出不含常见 mojibake marker。验证：`.\.venv-native\Scripts\python.exe -m py_compile app\agent\langgraph_loop.py tests\test_langgraph_action_workflows.py` 通过；定向回归 `$env:TMP='D:\tmp\pytest-tmp-native'; $env:TEMP='D:\tmp\pytest-tmp-native'; .\.venv-native\Scripts\python.exe -m pytest -q tests/test_langgraph_action_workflows.py -k "work_plan_missing_due_date or work_plan_context_cancel or work_plan_review_override_empty_tasks or native_work_plan"` -> `4 passed, 7 deselected`。本轮未修改 WebSocket/前端协议、DB schema、RAG corpus、scheduler、OCR 或前端代码；未 push；运行产物和 Chroma 文件仍保持未提交。

2026-06-20 追加更新：完成 course_maintenance LangGraph workflow 状态机迁移。本轮继续在 `D:\tmp\student-planner-langgraph-final` 的 `codex/langgraph-rag-submission` worktree 工作；开始记录 `git status --short --branch` = `## codex/langgraph-rag-submission...origin/codex/langgraph-rag-submission [ahead 12]`，HEAD = `8cb8691941e94e2dffac2c4b450c636f0da559ba`，既有未提交运行产物仍为 Chroma 文件、`logs/`、`student-planner/logs/`、旧 `agent_tool_node_*.db` 和 `student-planner/data/rag/chroma/rag_vectors.sqlite3`，未纳入本轮源码改动。实现：`langgraph_loop.py` 新增 `GraphCourseMaintenanceRuntime`、`course_maintenance` workflow state、`run_langgraph_course_maintenance_step()` 和 `run_langgraph_course_maintenance_workflow()`；rename/delete/merge 现在显式经过 `course_disambiguate -> ask_user_pause -> confirmed_write`，`list_courses` 结果、匹配 actions、review `PendingConfirmation`、首条与逐条 `DBWritePlan`、确认答案和 `update_course/delete_course` confirmed result 都进入 graph state。`loop.py` 的 course 本地分支改为调用新 workflow，旧 `_run_course_merge_shortcut` 仅保留兼容，不再是黄金路径依赖。`LANGGRAPH_MIGRATION_CONTRACTS.md` 已同步 course/workflow 状态边界；`tests/test_langgraph_action_workflows.py` 增强为 spy `run_langgraph_course_maintenance_step()` 并禁止旧 `_run_course_merge_shortcut`，新增取消不写库覆盖。验证：`.\.venv-native\Scripts\python.exe -m py_compile tests\test_langgraph_action_workflows.py app\agent\langgraph_loop.py app\agent\loop.py` 通过；focused course 回归 `8 passed`；用户指定 native 矩阵 `$env:TMP='D:\tmp\pytest-tmp-native'; $env:TEMP='D:\tmp\pytest-tmp-native'; .\.venv-native\Scripts\python.exe -m pytest -q tests/test_agent_contracts.py tests/test_langgraph_rag_runtime.py tests/test_langgraph_tool_node.py tests/test_langgraph_action_workflows.py tests/test_agent_db_write_gate.py tests/test_agent_loop.py tests/test_agent_loop_task_creation.py tests/test_agent_loop_schedule_import_shortcut.py tests/test_chat_ws.py tests/test_tool_preflight.py tests/test_tools_schema.py` -> `143 passed, 2 warnings in 144.77s`，warnings 仍是既有 Starlette/TestClient deprecation 与 APScheduler coroutine cleanup；真实 WS smoke `.\.venv-native\Scripts\python.exe scripts\langgraph_ws_smoke.py --db D:\tmp\student-planner-langgraph-course-workflow-smoke.db --stub-port 18196` 通过，course rename/delete/merge 序列分别为 `list_courses -> ask_user -> update_course/delete_course -> text -> done` 和 `ask_user -> list_courses -> ask_user -> delete_course -> text -> done`，可见 graph nodes 含 `course_disambiguate/ask_user_pause/confirmed_write` 且不含 `delegate_legacy_loop`，DB invariant 含 `Course Alpha Renamed`、`Course Beta` 删除、重复 `Course Gamma` 合并后只剩 1 条；study/work/schedule/task/RAG/no-web/plain_chat smoke 也保持通过。`git diff --check` 通过，仅 CRLF warning。本轮未修改 WebSocket/前端协议、DB schema、RAG corpus、scheduler、OCR 或前端代码；未 push。

2026-06-19 追加更新 6：完成 LangGraph action route-specific trace nodes 收口。本轮继续在 `D:\tmp\student-planner-langgraph-final` 的 `codex/langgraph-rag-submission` worktree 工作；既有 RAG Chroma 文件、`logs/`、`student-planner/logs/` 和旧 smoke DB 仍是运行产物，未纳入本轮提交。实现：`langgraph_loop.py` 的 Router Shell 从 action route terminal 扩展为 route-specific trace，`tool_workflow` 现在经过 `task_tool_node -> ask_user_pause -> confirmed_write`，`schedule_import` 经过 `schedule_parse -> ask_user_pause -> confirmed_write`，`study_plan/work_plan` 经过 `plan_review_write -> confirmed_write`，`course_maintenance` 经过 `course_disambiguate -> ask_user_pause -> confirmed_write`；`run_langgraph_agent_loop()` 仍复用 `run_agent_action_loop()` 执行真实工作流，只把 action `tool_result.result.graph_nodes` 带出用于 trace 证据，没有改变用户可见事件类型/顺序、确认后写库规则、前端协议、DB schema、scheduler、OCR 或 RAG corpus。回归：`tests/test_langgraph_action_workflows.py` 和 `tests/test_langgraph_rag_runtime.py` 新增/更新 graph trace 断言，覆盖 task/reminder、schedule import、study/work plan、course maintenance；focused LangGraph 回归 `43 passed`；用户指定 native 矩阵 `$env:TMP='D:\tmp\pytest-tmp-native'; $env:TEMP='D:\tmp\pytest-tmp-native'; .\.venv-native\Scripts\python.exe -m pytest -q tests/test_langgraph_action_workflows.py tests/test_langgraph_tool_node.py tests/test_chat_ws.py tests/test_agent_loop_task_creation.py tests/test_agent_loop_schedule_import_shortcut.py tests/test_tool_preflight.py tests/test_tools_schema.py` 为 `65 passed, 2 warnings in 147.29s`，warning 仍是既有 Starlette/TestClient deprecation 与 APScheduler coroutine cleanup。真实 WS golden smoke 命令 `.\.venv-native\Scripts\python.exe scripts\langgraph_ws_smoke.py --db D:\tmp\student-planner-langgraph-ws-smoke-route-nodes.db --stub-port 18125` 通过，新增 smoke evidence 证明 task/task_cancel/tool_failure/task_update 可见 `task_tool_node/ask_user_pause/confirmed_write`，schedule 可见 `schedule_parse/ask_user_pause/confirmed_write`，study/work 可见 `plan_review_write/confirmed_write`，course rename/delete/merge 可见 `course_disambiguate/ask_user_pause/confirmed_write`，所有可见 `graph_nodes` 均不含 `delegate_legacy_loop`，DB invariant 仍通过。

2026-06-19 追加更新 5：完成 ask_user pause/resume state 显式进入 LangGraph state 的收口。本轮继续在 `D:\tmp\student-planner-langgraph-final` 的 `codex/langgraph-rag-submission` worktree 工作；开始基线为 `git status --short --branch` = `## codex/langgraph-rag-submission...origin/codex/langgraph-rag-submission [ahead 6]`，HEAD = `8328148c43019fce4770838542bd01cf9cf62034`，upstream = `origin/codex/langgraph-rag-submission`；既有未提交运行产物仍包含 RAG Chroma 文件、`logs/`、`student-planner/logs/` 和旧 smoke DB，未纳入本轮提交。实现：`PlannerGraphState` 现在显式包含 `pending_ask`、`submitted_answer`、`pending_confirmation`、`pending_confirmation_answer`、`db_write_plan` 和 `resume_state`；`run_langgraph_tool_node()` 在 `ask_user` 暂停前写入 pending ask/resume state；WebSocket `asend` 恢复后通过 `resume_langgraph_ask_user_state()` 把用户答案、tool message、确认对象和确认答案写回 graph state；确认后的写库工具再从该 state 构造 `DBWritePlan`，覆盖 task/reminder、study/work plan、schedule import、course maintenance 的确认 id 和工具 scope 匹配。回归：新增/增强 `tests/test_langgraph_tool_node.py` 与 `tests/test_langgraph_action_workflows.py`，覆盖 pending ask before `ask_user`、submitted answer after resume、cancel no write、task/reminder confirmed scope、study/work plan、schedule import、course maintenance 和 tool failure recovery 的 graph-state 边界；定向组合 `tests/test_langgraph_tool_node.py tests/test_langgraph_action_workflows.py` 为 `13 passed`；用户指定 native 回归 `$env:TMP='D:\tmp\pytest-tmp-native'; $env:TEMP='D:\tmp\pytest-tmp-native'; .\.venv-native\Scripts\python.exe -m pytest -q tests/test_langgraph_action_workflows.py tests/test_agent_db_write_gate.py tests/test_chat_ws.py tests/test_agent_loop_task_creation.py tests/test_agent_loop_schedule_import_shortcut.py` 为 `38 passed, 2 warnings in 210.84s`，warnings 仍是既有 Starlette/TestClient deprecation 与 APScheduler coroutine cleanup；`git diff --check` 通过，仅 CRLF warning。真实 WS smoke 命令 `.\.venv-native\Scripts\python.exe scripts\langgraph_ws_smoke.py --db D:\tmp\student-planner-langgraph-ws-smoke-state.db --stub-port 18115` 通过；关键新增证据是 `task_cancel` 事件序列包含 `ask_user -> create_task -> text_delta/text -> done`，用户回答取消后 DB invariant 断言不存在 `Cancelled task`；完整 matrix 仍覆盖 no-web、RAG hit、RAG insufficient、plain chat streaming、tool failure recovery、task/reminder、study/work plan、schedule import、course rename/delete/merge，且可见 `graph_nodes` 不含 `delegate_legacy_loop`。本轮未修改 WebSocket/前端协议、确认卡文案/结构、数据库 schema、持久 session 表、RAG 语料、scheduler、OCR 或前端代码，因此未跑 frontend test/typecheck；未 push。剩余兼容边界：`delegate_legacy_loop` 和 legacy helper 仍保留给非 golden/兼容路径，低层 `execute_tool` 仍是非 agent caller 的直接 dispatcher，但 LangGraph runtime 的 golden action path 已用 graph state 表达 pause/resume 与确认写库状态。

2026-06-19 追加更新 4：完成 production generic action loop 复用 LangGraph tool node 的收口。本轮继续在 `D:\tmp\student-planner-langgraph-final` 的 `codex/langgraph-rag-submission` worktree 工作；开始基线为 `git status --short --branch` = `## codex/langgraph-rag-submission...origin/codex/langgraph-rag-submission [ahead 5]`，HEAD = `5f46132c11ce7e8b4e44104ba4393445f8487fe3`，upstream = `origin/codex/langgraph-rag-submission`，既有未提交运行产物仍包含 RAG Chroma 文件、`logs/`、`student-planner/logs/` 和旧本地 smoke DB，未纳入本轮提交。实现：`run_agent_action_loop()` 的 generic model tool-call 分支不再手写 unknown-tool guard、ask_user guard、retry、task/schema preflight、reminder argument repair、tool execution、tool_result、压缩摘要、AgentLog、tool message、`tool_history/error_count/last_free_slots_result` 更新，而是把 pending tool call、messages、preflight texts 和 step state 交给 `run_langgraph_tool_node()`；外层只保留必须依赖 WebSocket `asend` 的 ask_user 用户回复接收、确认写入 state 构造，以及 study/work context 补齐等多轮 orchestration。`run_langgraph_tool_node()` 同步复用 legacy `_normalize_ask_type()`，保持 confirm 缺少 options/data 时降级 review 的协议行为。新增回归在 `tests/test_langgraph_action_workflows.py` spy `run_langgraph_tool_node()`，证明 task/reminder production path 3 次进入 graph tool node 且事件序列和 DB invariant 不变；`scripts/langgraph_ws_smoke.py` 将 work-plan smoke 的固定 `2099-06-10` 截止日改为运行日 + 21 天，避免真实 smoke 在本地 shortcut 中查询几十年空闲时间。验证：先加失败回归，最初 task/reminder 测试保持功能通过但 `tool_node_spy.await_count == 0`；实现后该测试通过。`tests/test_langgraph_tool_node.py` 为 `7 passed`，`tests/test_langgraph_action_workflows.py` 为 `5 passed`；用户指定 native venv 完整相关回归加新增 LangGraph tests：`$env:TMP='D:\tmp\pytest-tmp-native'; $env:TEMP='D:\tmp\pytest-tmp-native'; .\.venv-native\Scripts\python.exe -m pytest -q tests/test_agent_contracts.py tests/test_langgraph_rag_runtime.py tests/test_agent_loop.py tests/test_agent_loop_task_creation.py tests/test_agent_loop_schedule_import_shortcut.py tests/test_chat_ws.py tests/test_tool_preflight.py tests/test_tools_schema.py tests/test_langgraph_tool_node.py tests/test_langgraph_action_workflows.py` 为 `135 passed, 2 warnings in 261.88s`，warnings 是既有 Starlette/TestClient deprecation 和 APScheduler coroutine cleanup；`git diff --check` 通过，仅 CRLF warning。真实 `127.0.0.1:8001` + `SP_AGENT_RUNTIME=langgraph` WS smoke 命令 `.\.venv-native\Scripts\python.exe scripts\langgraph_ws_smoke.py --db D:\tmp\student-planner-langgraph-ws-smoke-tool-node.db --stub-port 18105` 通过；关键证据：RAG hit graph_nodes = `route -> retrieve_rag -> compose_runtime_hints -> rag_qa`，RAG insufficient graph_nodes = `route -> retrieve_rag -> rag_insufficient`，study/work graph_nodes = `route -> retrieve_rag -> compose_runtime_hints -> study_plan`，所有可见 graph_nodes 均不含 `delegate_legacy_loop`；plain chat 事件为 `text_delta -> text -> done`；tool failure recovery 写入 `Recovered task`；task update 将 `Smoke task` 更新到 `2099-06-01 16:00-17:00` 且 reminder `advance=15 / 15:45`；study 写入 `English 3` 复习任务；work 写入 5 条 `ML report` 作业任务；schedule import 写入 6 门课程；course rename/delete/merge invariant 均通过。本轮未修改 WebSocket/前端协议、数据库 schema、RAG corpus、scheduler、OCR 或前端代码，因此未跑 frontend test/typecheck；未 push。剩余兼容 fallback：`delegate_legacy_loop` 仍保留给非 golden/未来未知 route，`run_agent_loop()` 仍是 legacy-compatible wrapper，local shortcuts 仍按现有产品语义保留。

2026-06-19 追加更新 3：完成 LangGraph golden matrix 去 legacy delegate 收口。本轮仍在 `D:\tmp\student-planner-langgraph-final` 的 `codex/langgraph-rag-submission` worktree 工作；开始基线为 `git status --short --branch` = `## codex/langgraph-rag-submission...origin/codex/langgraph-rag-submission [ahead 4]`，HEAD = `80713e731cf3c4c0596d3b0fd0985e1c9ddd5a39`，upstream = `origin/codex/langgraph-rag-submission`，既有未提交运行产物仍包含 RAG Chroma 文件、`logs/`、`student-planner/logs/` 和旧本地 smoke DB，未纳入提交。
实现：`langgraph_loop.py` 新增 `rag_qa`、`plain_chat` graph terminal nodes，`SP_AGENT_RUNTIME=langgraph` 下 RAG hit 和 plain chat 走新增 `run_agent_text_loop()`，action routes 继续走 `run_agent_action_loop()`；`delegate_legacy_loop` 保留为明确兼容 fallback，但 golden matrix 不再进入它。`LANGGRAPH_MIGRATION_CONTRACTS.md` 已同步 Router Shell V2 Mermaid 和 fallback 边界；`tests/test_langgraph_rag_runtime.py`、`tests/test_langgraph_action_workflows.py` 增强 no-web、RAG hit、RAG insufficient、plain chat streaming、task/reminder、study/work plan、schedule import、course maintenance、tool failure recovery、ask_user multiturn 的 non-delegate 覆盖；`scripts/langgraph_ws_smoke.py` 扩成完整真实 WS golden smoke，并记录事件序列、可见 graph_nodes、最终文本和 DB invariant。
验证：聚焦 LangGraph/contract 回归 `tests/test_langgraph_rag_runtime.py tests/test_langgraph_action_workflows.py tests/test_agent_contracts.py` 为 `54 passed`；用户指定完整 native 后端回归加新增 LangGraph tests 为 `135 passed, 2 warnings in 135.88s`，warnings 仍是既有 Starlette/TestClient deprecation 和 APScheduler coroutine cleanup；`git diff --check` 通过，仅 CRLF warning。真实 `127.0.0.1:8001` + `SP_AGENT_RUNTIME=langgraph` WS smoke 命令 `.\.venv-native\Scripts\python.exe scripts\langgraph_ws_smoke.py --db D:\tmp\student-planner-langgraph-ws-smoke-full.db --stub-port 18095` 通过；关键证据：RAG hit graph_nodes = `route -> retrieve_rag -> compose_runtime_hints -> rag_qa`，RAG insufficient graph_nodes = `route -> retrieve_rag -> rag_insufficient`，study/work graph_nodes = `route -> retrieve_rag -> compose_runtime_hints -> study_plan`，所有可见 graph_nodes 均不含 `delegate_legacy_loop`；plain chat 事件为 `text_delta -> text -> done`；tool failure recovery 最终写入 `Recovered task`；DB invariant 包含 `Smoke task` 更新到 `2099-06-01 16:00-17:00` 且 reminder `advance=15 / 15:45`、`English 3` 复习任务、5 条 `ML report` 作业任务、课表导入 6 门课程、`Course Alpha Renamed`、`Course Beta` 删除、重复 `Course Gamma` 合并后只剩 1 条。
本轮未修改 WebSocket/前端协议、数据库 schema、RAG 语料、scheduler、OCR 或前端代码，因此未跑 frontend test/typecheck；未 push。剩余兼容 fallback：`delegate_legacy_loop` 仍保留给非 golden/未来未知 route 兼容，`run_agent_loop()` 仍是 legacy-compatible wrapper，底层 action helper 和 text helper 后续可继续拆成更细 LangGraph nodes。

2026-06-19 追加更新 2：补齐 LangGraph action-route 真实 WebSocket golden smoke。当前继续在 `D:\tmp\student-planner-langgraph-final` 的 `codex/langgraph-rag-submission` worktree 工作；继续保留既有未提交运行时产物 `student-planner/data/rag/chroma/chroma.sqlite3`、`logs/`、`student-planner/logs/` 和旧本地 smoke DB 不纳入提交。新增可复用 smoke harness：`student-planner/scripts/langgraph_ws_smoke.py`，它使用临时 SQLite DB、Alembic head、本地 OpenAI-compatible stub、真实 uvicorn `127.0.0.1:8001` 和 `SP_AGENT_RUNTIME=langgraph` 跑 `/ws/chat`，并在结束后检查 DB invariant；该脚本只把临时 DB 写到 `D:\tmp`，不提交运行时产物。

真实 WS smoke 已通过：命令 `.\.venv-native\Scripts\python.exe scripts\langgraph_ws_smoke.py --db D:\tmp\student-planner-langgraph-ws-smoke-full.db --stub-port 18095` 返回 0。事件证据：task create + reminder 为 `ask_user -> create_task -> text -> done`；task update reminder 为 `list_tasks -> ask_user -> update_task -> text_delta -> text -> done`；study plan 为 `rag_retrieve_study_materials -> get_free_slots -> create_study_plan -> ask_user -> create_task -> result -> text -> done`；work plan 为 `rag_retrieve_study_materials -> get_free_slots -> create_work_plan -> ask_user -> create_task... -> result -> text -> done`；schedule import 为 `parse_schedule -> ask_user -> save_period_times -> ask_user -> bulk_import_courses -> text -> done`；course rename 为 `list_courses -> ask_user -> update_course -> text -> done`；course delete 为 `list_courses -> ask_user -> delete_course -> text -> done`；course merge 为 `ask_user -> list_courses -> ask_user -> delete_course -> text -> done`。DB invariant：`Smoke task` 被更新到 `2099-06-01 16:00-17:00`，reminder 为 `advance=15 / 15:45`；`English 3 - 基础覆盖` 复习任务写入；`ML report 作业` 5 条作业计划任务写入；课表导入写入 `高等数学/线性代数/大学英语/概率论/大学物理/体育`；`Course Alpha Renamed` 存在，`Course Beta` 删除，重复 `Course Gamma` 合并后只剩 1 条。最终 native 回归 `$env:TMP='D:\tmp\pytest-tmp-native'; $env:TEMP='D:\tmp\pytest-tmp-native'; .\.venv-native\Scripts\python.exe -m pytest -q tests/test_agent_contracts.py tests/test_langgraph_rag_runtime.py tests/test_agent_loop.py tests/test_agent_loop_task_creation.py tests/test_agent_loop_schedule_import_shortcut.py tests/test_chat_ws.py tests/test_tool_preflight.py tests/test_tools_schema.py tests/test_langgraph_action_workflows.py` 为 `125 passed, 2 warnings in 131.02s`；`git diff --check` 通过，仅 CRLF warning。本轮仍未改变前端协议、确认卡结构、DB schema、scheduler、OCR、RAG corpus 或外部服务依赖。

2026-06-19 追加更新：在 `D:\tmp\student-planner-langgraph-final` 完成 LangGraph action-route 收口提交前验证。开始记录：`git status --short --branch` = `## codex/langgraph-rag-submission...origin/codex/langgraph-rag-submission [ahead 2]`，HEAD = `8ac84a656833d485338efa56d5cc45699978d8ef`，upstream = `origin/codex/langgraph-rag-submission`；既有未提交运行时产物仍包含 `student-planner/data/rag/chroma/chroma.sqlite3`、`logs/`、`student-planner/logs/` 和若干本地 smoke DB，未纳入本轮提交。实现：`langgraph_loop.py` 增加 `tool_workflow`、`schedule_import`、`study_plan`、`course_maintenance` action terminal nodes，action route 的 `graph_nodes` 不再包含 `delegate_legacy_loop`；`loop.py` 将原 action loop 抽为 `run_agent_action_loop()`，保留 legacy-compatible `run_agent_loop()` fallback；`contracts.py` 补齐 `改到/改成/改为/提前` action marker，避免“改任务提醒”误走 plain chat。新增 `tests/test_langgraph_action_workflows.py` 覆盖 task/reminder、work plan、schedule import、course maintenance 四类 confirmed write 的事件序列和 DB invariant，并断言 action route 不调用 legacy delegate；`tests/test_langgraph_rag_runtime.py` 同步 Router Shell V2/action node trace 断言。

验证：定向 `tests/test_langgraph_action_workflows.py tests/test_langgraph_rag_runtime.py` 为 `40 passed`；用户指定 native venv 回归加新增 action graph tests：`$env:TMP='D:\tmp\pytest-tmp-native'; $env:TEMP='D:\tmp\pytest-tmp-native'; .\.venv-native\Scripts\python.exe -m pytest -q tests/test_agent_contracts.py tests/test_langgraph_rag_runtime.py tests/test_agent_loop.py tests/test_agent_loop_task_creation.py tests/test_agent_loop_schedule_import_shortcut.py tests/test_chat_ws.py tests/test_tool_preflight.py tests/test_tools_schema.py tests/test_langgraph_action_workflows.py` 为 `125 passed, 2 warnings in 136.14s`，warning 仍是既有 Starlette/TestClient deprecation 与 APScheduler coroutine cleanup；`git diff --check` 通过，仅有 CRLF replacement warning。真实 `127.0.0.1:8001` + `SP_AGENT_RUNTIME=langgraph` WS smoke：用临时 DB、Alembic head、OpenAI-compatible local stub 跑通 task create + reminder，事件为 `connected -> ask_user -> create_task -> text_delta/text -> done`，DB invariant 为 task `Smoke task` 和 reminder `advance=30` 写入；在后续 task update/study/work 多轮 smoke 中，临时 LLM stub 对确认恢复后的 messages 重建误判并触发 loop 上限或错误，因此本轮没有把完整 WS golden matrix 记为通过。代码级回归已覆盖完整四类 action DB invariant；后续若继续补 live smoke，应改用更稳定的 Playwright/Node harness 或真实 provider，而不是 PowerShell inline stub。

本轮未改变前端协议、用户可见确认卡结构、数据库 schema、scheduler、OCR、RAG corpus，也未引入外部检索/服务；`LANGGRAPH_MIGRATION_CONTRACTS.md` 已更新为 Router Shell V2/action-node 现状。

2026-06-18 追加更新：完成通用 code-level DB write gate，本轮只在 `D:\tmp\student-planner-langgraph-final` 的 `codex/langgraph-rag-submission` worktree 工作。开始记录为 `git status --short --branch` = `## codex/langgraph-rag-submission...origin/codex/langgraph-rag-submission`，HEAD = `f1f3a2cb73e6e841521637a8a86cb26e6eceb26e`，upstream = `origin/codex/langgraph-rag-submission`；既有未提交运行产物仍为 `student-planner/data/rag/chroma/chroma.sqlite3`、`logs/`、`student-planner/logs/`，未纳入本轮提交。根因：上一轮 schedule import pilot 已有 `PendingConfirmation + DBWritePlan`，但其它写库入口仍存在确认后直接 `execute_tool()` 的路径，尤其是 `create_task`、`update_task`、`set_reminder`、`update_course/delete_course`、study/work plan 写任务，以及 orphan `review_override=` fallback；这些路径如果迁到 LangGraph 后仍只依赖 prompt/event 语义，会缺少代码级 confirmation id、route、tool scope 和 answer 匹配校验。实现：在 `PendingConfirmation` 增加 `allowed_tool_names`，在 `loop.py` 增加 `_build_pending_confirmation()`、`_build_db_write_plan()`、`_execute_confirmed_db_write_plan()` 的通用校验，统一检查缺 pending、缺 plan、confirmation id 不匹配、route 不匹配、tool 不在允许 scope、取消/非写入授权 answer；将本地 task create、confirmed study/work plan `create_task`、plan adjustment `update_task`、course maintenance `update_course/delete_course`、generic tool loop 中的写库工具纳入该 gate。`execute_tool` 仍保持给非 agent caller 使用的 direct dispatcher；schedule import 的 `save_period_times` 仍是确认前信息补全写入偏好，不在本轮改变产品语义。`review_override=` orphan fallback 改为返回“确认状态已失效”错误且不写库。迁移文档已同步为 `agent_loop_write_tools` pilot，并明确后续 LangGraph node 必须保留同一 gate。

新增回归：`tests/test_agent_db_write_gate.py` 覆盖 `create_task`、`update_task`、`set_reminder`、`bulk_import_courses`、`update_course`、`delete_course` 缺少 `pending_confirmation/db_write_plan` 时不写库，route 不匹配不写库，同一确认允许 `create_task + set_reminder` scope 链路，以及 tool scope 不匹配不写库；`tests/test_langgraph_rag_runtime.py` 更新 orphan `review_override=` 用例为不写库错误路径。验证：新增 gate 测试 `4 passed`；重点组合 `tests/test_agent_contracts.py tests/test_langgraph_rag_runtime.py tests/test_agent_loop.py tests/test_agent_loop_task_creation.py tests/test_agent_loop_schedule_import_shortcut.py tests/test_agent_db_write_gate.py` 为 `94 passed`；用户指定 native venv 后端回归加新增 gate 测试 `$env:TMP='D:\tmp\pytest-tmp-native'; $env:TEMP='D:\tmp\pytest-tmp-native'; .\.venv-native\Scripts\python.exe -m pytest -q tests/test_agent_contracts.py tests/test_langgraph_rag_runtime.py tests/test_agent_loop.py tests/test_agent_loop_task_creation.py tests/test_agent_loop_schedule_import_shortcut.py tests/test_chat_ws.py tests/test_tool_preflight.py tests/test_tools_schema.py tests/test_agent_db_write_gate.py` 最终为 `124 passed, 1 warning in 101.74s`；首次同一组合在 `test_chat_ws.py::test_ws_returns_error_for_orphan_answer_payload` teardown 遇到一次 transient SQLite `database is locked`，清理 `test.db` 后原命令重跑通过，符合既有环境坑。`git diff --check` 通过，仅 Git 提示若干 CRLF replacement warning。

真实 WebSocket smoke：已执行 Alembic 到 head，并以 `SP_AGENT_RUNTIME=langgraph` 启动 `127.0.0.1:8001`；临时账号 `gate-smoke-1781778859871` 通过 `/ws/chat` 与真实 HTTP API 验证。创建任务+提醒事件为 `connected -> rag_retrieve_study_materials -> ask_user -> ask_user -> create_task -> text -> done`，HTTP invariant 为 tasks `+1`、reminders `+1`；今晚复习计划确认写任务事件为 `connected -> rag_retrieve_study_materials -> get_free_slots -> ask_user -> create_task -> create_task -> result -> text -> done`，HTTP invariant 为 tasks `+2`；课表导入 `tests/fixtures/sample_schedule.xlsx` 事件为 `connected -> parse_schedule -> ask_user -> save_period_times -> ask_user -> save_period_times -> ask_user -> bulk_import_courses -> text -> done`，HTTP invariant 为 courses `6`，样例课程包含 `高等数学/线性代数/大学英语/大学物理/概率论/体育`；课程改名事件为 `connected -> list_courses -> ask_user -> update_course -> text -> done`，DB invariant 为 `高等数学A` 存在；课程删除事件为 `connected -> list_courses -> ask_user -> delete_course -> text -> done`，DB invariant 为 `线性代数` 不再存在、剩余 courses `5`。本轮未改 WebSocket 协议、前端协议、数据库 schema、提醒调度规则、RAG 语料或 Chroma；未 push。

2026-06-15 追加更新：完成 Confirmation State V1 的 schedule import pilot。开始时 `git status --short --branch` 为 `## codex/langgraph-rag-submission`，HEAD 为 `9ff8829d246c49d0000ecbfd6fd0d1f7b8880c96`，除本轮改动外仍只有既有运行产物 `student-planner/data/rag/chroma/chroma.sqlite3`、`logs/`、`student-planner/logs/` 未提交。根因：课表导入本地 shortcut 虽然事件流上已经是 `parse_schedule -> ask_user -> bulk_import_courses`，但最终写入段只是确认后直接组 `import_args` 调 `execute_tool("bulk_import_courses")`，没有代码层的 `pending_confirmation/db_write_plan` 状态对象和匹配校验；这会让 LangGraph 化后的确认票据语义继续停留在 prompt/event 约束。实现：在 `app/agent/contracts.py` 新增 `PendingConfirmation`、`DBWritePlan` 和 `CONFIRMATION_STATE_V1_PILOTS=("schedule_import",)`；在 `app/agent/loop.py` 为课表导入最终 review 构造 `pending_confirmation + db_write_plan`，取消/否定仍直接 `text -> done` 且不执行写入，确认后才通过 `_execute_confirmed_db_write_plan()` 校验 confirmation id 与 tool name，再调用 `bulk_import_courses`；`execute_tool` 本身仍是 direct dispatcher，非 pilot 写工具仍未统一迁到 central confirmation ticket。测试：先加失败回归，最初 `tests/test_agent_loop_schedule_import_shortcut.py` 因 `ImportError: cannot import name 'DBWritePlan'` 失败；实现后 `$env:TMP='D:\tmp\pytest-tmp-native'; $env:TEMP='D:\tmp\pytest-tmp-native'; .\.venv-native\Scripts\python.exe -m pytest -q tests/test_agent_contracts.py tests/test_agent_loop_schedule_import_shortcut.py` 为 `15 passed`，新增/更新覆盖确认前不写、取消不写、确认后以 `PendingConfirmation/DBWritePlan` 执行、缺少 confirmation state 返回错误不落库。用户指定 native venv 回归 `$env:TMP='D:\tmp\pytest-tmp-native'; $env:TEMP='D:\tmp\pytest-tmp-native'; .\.venv-native\Scripts\python.exe -m pytest -q tests/test_agent_contracts.py tests/test_langgraph_rag_runtime.py tests/test_agent_loop.py tests/test_chat_ws.py tests/test_tool_preflight.py tests/test_tools_schema.py` 为 `98 passed, 1 warning`，warning 仍是既有 Starlette/TestClient deprecation。真实 WS smoke：已执行 Alembic 到 head，启动 `127.0.0.1:8001`，显式 `SP_AGENT_RUNTIME=langgraph`，临时用户上传 `tests/fixtures/sample_schedule.xlsx` 后通过 `/ws/chat` 发送 `please import this schedule file_id=...`，事件为 `connected -> tool_call(parse_schedule) -> tool_result(parse_schedule) -> ask_user -> tool_call(bulk_import_courses) -> tool_result(bulk_import_courses) -> text -> done`，最终文本 `课表已导入完成，共 6 条。`，`GET /api/courses/` 返回 `courses_count=6`，前 3 门为 `高等数学`、`线性代数`、`大学英语`。本轮未修改 WebSocket/frontend 协议、前端代码、数据库 schema、RAG 语料或 Chroma 数据库；因此未跑 frontend test/typecheck。下一步建议：把同一 `pending_confirmation/db_write_plan` gate 迁到 study/work plan 的 `create_task` 写入或 course maintenance 的 update/delete，再考虑抽共享 LangGraph write-tool node。

2026-06-15 追加更新：完成 LangGraph Router Shell V1。此前 `langgraph_loop.py` 的真实图仍是线性 `load_context -> retrieve -> compose_hints -> END`，RAG gate/no-web/delegate 都藏在节点内部或 wrapper 代码里，图本身看不出 agent loop 的路由结构。本轮最小改动仍不迁完整 `run_agent_loop()`，把 graph 改为显式 conditional-edge shell：`route` 根据 `decide_agent_route()` 写入 `route/route_reason/should_retrieve/should_gate_rag_answer/retrieval_mode`；`route` 条件分流到 `no_web`、`retrieve_rag` 或 `delegate_legacy_loop`；`retrieve_rag` 后只有 `should_gate_rag_answer=true` 且 `evidence_sufficient=false` 才进入 `rag_insufficient`，其他路径进入 `compose_runtime_hints -> delegate_legacy_loop`。工具、课表导入、任务/提醒、课程维护、复习/作业计划、`ask_user` 暂停恢复、tool preflight 和写库仍全部委托 legacy Agent Loop，确认后落库规则未变。新增 `get_langgraph_router_shell_mermaid()`，当前图证据为 `__start__ --> route`，`route -.-> no_web/retrieve_rag/delegate_legacy_loop`，`retrieve_rag -.-> rag_insufficient/compose_runtime_hints`，`compose_runtime_hints --> delegate_legacy_loop`，三个终点进入 `__end__`。新增/更新回归覆盖 graph Mermaid 节点、no-web 在 graph 内短路且不 RAG/不 legacy delegate、RAG 命中走 retrieve + compose hints + delegate、RAG insufficient 走 retrieve + gate 且不 delegate、`下周四有大学英语3考试，帮我安排一下` 保持 `study_plan` route 并允许非 gating RAG side-channel、课表导入/提醒/课程维护 route 不被 RAG gate 抢走。验证：开始时 `git status --short --branch` 为 `## codex/langgraph-rag-submission`，仅有既存运行产物 `student-planner/data/rag/chroma/chroma.sqlite3`、`logs/`、`student-planner/logs/` 脏；HEAD 为 `49531920eee3a2d2f44cf6e99a3453c4dd93ff5f`。定向 `tests/test_agent_contracts.py tests/test_langgraph_rag_runtime.py` 为 `45 passed`；用户指定 native venv 回归 `$env:TMP='D:\tmp\pytest-tmp-native'; $env:TEMP='D:\tmp\pytest-tmp-native'; .\.venv-native\Scripts\python.exe -m pytest -q tests/test_agent_contracts.py tests/test_langgraph_rag_runtime.py tests/test_agent_loop.py tests/test_chat_ws.py tests/test_tool_preflight.py tests/test_tools_schema.py` 为 `97 passed, 1 warning`（既有 Starlette/TestClient deprecation）。本轮未改 WebSocket 事件协议或 chat runtime selector，因此未做真实 WS smoke；未触碰前端协议，因此未跑 frontend test/typecheck；未扩 RAG 语料，未重建或提交 Chroma 数据库。下一步建议迁 `pending_confirmation/db_write_plan` 的 Confirmation State V1，先选一个最小写库路径做代码级确认票据，不要直接迁完整工具 loop。

2026-06-14 追加更新：完成 LangChain/LangGraph 全量迁移前置 contracts 梳理与落地，本轮没有迁移整个 `run_agent_loop()`，只把后续迁移不能再隐式依赖的规则固化为代码、测试和文档。新增 `student-planner/app/agent/contracts.py`，定义 `AgentRoute`、`RouteDecision`、`decide_agent_route()`、state/event/confirmation/tool/streaming/golden-matrix 常量，明确 `no_web -> rag_qa/rag_insufficient -> schedule_import/course_maintenance/study_plan/tool_workflow -> plain_chat` 的优先级，并把最终 route 与 RAG retrieval side-channel 分开：计划/工具路径仍可 `should_retrieve=true` 保留当前 `rag_retrieve_study_materials` 事件和 context hint，但 `should_gate_rag_answer=false`，不会被 RAG evidence gate 抢走。RAG 候选且 evidence 不足分类为 `rag_insufficient`。新增 `student-planner/app/agent/LANGGRAPH_MIGRATION_CONTRACTS.md`，记录架构边界：LangGraph 后续负责 orchestration/state/edge/暂停确认/事件顺序，LangChain chain 或 Runnable 仍作为 graph node 内部的 LLM、prompt、parser、retriever、tool-binding 实现单元；当前 runtime 仍是 LangGraph RAG/context 前置 + legacy Agent Loop 工具闭环。文档同时固化 Router、State Schema、Event Contract（含 `text_delta/text/result/tool_call/tool_result/ask_user/done/error`）、Confirmation Contract、Tool Boundary、Streaming Strategy 和 Golden E2E Matrix，并记录 `execute_tool` 当前没有统一确认票据硬拦截，现有安全来自 shortcut/prompt/preflight/tests，后续 native graph 迁移必须把写库确认做成 `pending_confirmation/db_write_plan` 状态硬约束而不是 prompt-only 约束。新增 `tests/test_agent_contracts.py` 覆盖 no-web 优先、RAG evidence gate 分类、RAG 命中分类、考试安排走 study/tool 路径且与当前 `_load_context_node()` 的 `should_retrieve/should_gate_rag_answer` 对齐、课表导入/提醒/课程维护分类、事件字段、确认工具集合、state/golden matrix 覆盖面。验证：先记录 `git status --short --branch` 为 `## codex/langgraph-rag-submission`，仅有既存运行产物 `student-planner/data/rag/chroma/chroma.sqlite3`、`logs/`、`student-planner/logs/` 脏；HEAD 为 `6ea1367e609dcdf222fb9dd67c8d9149efc8cc24`。新增定向测试 `$env:TMP='D:\tmp\pytest-tmp-native'; $env:TEMP='D:\tmp\pytest-tmp-native'; .\.venv-native\Scripts\python.exe -m pytest -q tests/test_agent_contracts.py` 为 `10 passed`；用户指定 native venv 回归加上 contract 测试 `$env:TMP='D:\tmp\pytest-tmp-native'; $env:TEMP='D:\tmp\pytest-tmp-native'; .\.venv-native\Scripts\python.exe -m pytest -q tests/test_agent_contracts.py tests/test_langgraph_rag_runtime.py tests/test_agent_loop.py tests/test_chat_ws.py tests/test_tool_preflight.py tests/test_tools_schema.py` 为 `90 passed, 2 warnings`（既有 Starlette/TestClient deprecation，另有一次 SQLAlchemy/aiosqlite connection cleanup warning；测试均通过）。未触碰前端协议、事件消费结构或 chat 路由选择，因此未跑 frontend test/typecheck，也不需要额外真实 WebSocket smoke；未扩 RAG 语料，未重建或提交 Chroma 数据库。后续 LangGraph-native 迁移建议顺序：先把这些 contracts 变成 graph state/conditional edges，再迁 no-web/RAG gate，再迁 confirmation pause/tool node，最后迁 legacy local shortcuts 和 streaming 细节。

2026-06-14 追加更新：完成 LangGraph/RAG Evidence Gate P1。根因是 LangGraph 只在召回到 RAG context 时给内层 Agent Loop 注入 hint；一旦问题被判定为 RAG 候选但本地证据为空或弱相关，旧链路仍会继续委托普通 LLM，自由回答“量子退相干错误”等知识库未覆盖问题。本轮最小改动保留现有 LangGraph/Agent Loop 架构：`app/agent/rag.py` 为 RAG 结果新增 evidence metadata，只把通过 evidence gate 的 hit 注入 context；gate 同时检查 score、整体 query 覆盖、分段覆盖和连续缺失关键字符，避免“机器学习资料覆盖很强但火星殖民完全缺失”这类主题重叠误判；hash fallback 改为稳定 `sha256` 桶位，并把内部 evidence candidate pool 扩到 `candidate_top_k=10`，避免本地 fallback 因 top3 排序抖动漏掉真实证据，但公开 `hits/count/sources` 和 runtime context 仍限制在请求的 `top_k=3`。`app/agent/langgraph_loop.py` 在 RAG 候选且 evidence 不足时由代码直接保存并返回“当前知识库没有足够资料，无法基于本地资料可靠回答这个问题。”，不再进入普通 LLM；no-web guard 优先于 RAG；“复习计划/帮我安排/安排一下/提醒/课表”等工具 workflow 继续绕过知识问答 gate，保持确认后落库规则不变。新增/更新回归覆盖 RAG 命中可答、无命中/低相关不调用普通 LLM、`二战后的国际秩序安排是什么` 这类含“安排”的知识问答仍被 gate、`下周四有大学英语3考试，帮我安排一下` 仍走 Agent Loop、实时公共事件继续 no-web 短路，以及 `top_k=3` 时公开 hits 和 context 不超过 3。验证：native venv 下用户指定命令 `$env:TMP='D:\tmp\pytest-tmp-native'; $env:TEMP='D:\tmp\pytest-tmp-native'; .\.venv-native\Scripts\python.exe -m pytest -q tests/test_langgraph_rag_runtime.py tests/test_agent_loop.py tests/test_chat_ws.py tests/test_tool_preflight.py tests/test_tools_schema.py` 最终为 `80 passed, 1 warning`（warning 为既有 Starlette/TestClient deprecation；前一轮曾遇到 transient sqlite `test.db` locked，原命令重跑通过）。已重启 `127.0.0.1:8001`，显式 `SP_AGENT_RUNTIME=langgraph`，真实 WebSocket smoke：RAG 命中“改革开放是什么时候开始的”为 `connected -> tool_call -> tool_result -> text_delta... -> text -> done`，`count=3`、`candidate_count=10`、`evidence_sufficient=true`、`evidence_count=7`、`evidence_context_count=3`、`requested_top_k=3`、`candidate_top_k=10`、`evidence_top_coverage=1.0`、`evidence_top_segment_coverage=1.0`、`evidence_top_unmatched_run=1`，流式 delta 33 条且回答含 1978；RAG 不覆盖“机器学习里的量子退相干错误怎么解释”为 `connected -> tool_call -> tool_result -> text -> done`，`count=3`、`candidate_count=10`、`evidence_sufficient=false`、`evidence_count=0`、`evidence_context_count=0`、`evidence_top_coverage=0.6364`、`evidence_top_segment_coverage=0.4286`、`evidence_top_unmatched_run=7`，无 `text_delta`，直接返回固定资料不足文案；实时问题“最新政策是什么”为 `connected -> text -> done`，`tool_names=[]`，返回没有联网检索说明。本轮未触碰前端协议，因此未跑 frontend test/typecheck；未扩 RAG 语料，未重建或提交 Chroma 数据库。

2026-06-14 追加更新：修复聊天在没有联网工具时仍回答“最新/近期公共事件”的幻觉边界问题。用户截图复现“ 中国最新的大事件有什么 ”时，助手列出 2026 年 5 月神舟二十号、人工智能法、一带一路论坛等，并声称“不是知识库，而是公开权威可验证信息”。经确认当前 Agent 工具 schema 没有 web/search/news/browser，只有课程、任务、提醒、课表解析、memory 与本地 RAG；该回答不是工具调用结果，而是 text-only 纯 LLM 路径用模型自身知识/臆测生成，原系统提示只禁止编造课程、考试、教室和用户偏好，没有约束“最新新闻/公共事件不能凭模型知识回答”。本轮修复：`app/agent/loop.py` 增加实时公共信息请求的确定性短路，遇到“最新/最近/当前/今天/2026”等时间窗口 + “新闻/大事/中国/国际/政策/科技/航天”等公共事件域，且不是本地任务/课表/提醒工具意图时，直接返回“没有联网检索、新闻搜索或网页浏览工具，不能可靠回答实时公共事件”，不再调用 LLM；`Agent.md` 和 `RESPONSE_FORMAT_RULES` 同步加入无联网工具边界，禁止声称来自公开权威来源。新增回归 `test_current_public_event_request_does_not_call_llm_or_claim_web` 和 `test_response_format_rules_reject_unsourced_current_news_answers`。验证：native venv 下定向 + 流式保护测试 `4 passed`；核心组合 `tests/test_agent_loop.py tests/test_chat_ws.py tests/test_tool_preflight.py tests/test_tools_schema.py tests/test_langgraph_rag_runtime.py` 为 `69 passed, 1 warning`。已重启 `127.0.0.1:8001` 并以 `SP_AGENT_RUNTIME=langgraph` 启动，真实 WebSocket 临时账号复测“ 中国最新的大事件有什么 ”，事件为 `text -> done`，`tool_names=[]`，`error=None`，返回明确无联网能力说明。

2026-06-14 追加更新：修复 LangGraph/RAG 回复“事件顺序像流式、实际仍等最终 response 后批量 replay”的问题。用户指出前端仍没有真实流式输出后，新增带闸门的回归测试确认根因：`run_agent_loop()` 收到 `content_delta` 后只写入 `streamed_deltas`，直到 OpenAI-compatible stream 发出最终 `response` 才把 delta 一次性 yield 给 WebSocket；因此前端虽然收到 `text_delta -> text`，体验上仍是最终回答生成完才显示。本轮修复在 `app/agent/loop.py` 增加 text-only stream 判定：纯文本问答和 LangGraph/RAG runtime hint 的知识问答不再携带 tools，收到 `content_delta` 立即转发；课程、任务、提醒、复习计划、作业计划、导入、记忆写入等可能需要 tool call 的请求仍保留工具定义并缓存 preamble，避免把“我先查一下”这类工具调用前言提前显示。新增回归 `test_plain_chat_streams_delta_before_response_finishes` 和 `test_rag_runtime_hint_streams_delta_before_response_finishes`，用 `asyncio.Event` 卡住最终 response，要求首个 `text_delta` 必须在 response 解闸前出现；工具前言保护 `test_streaming_preamble_is_not_emitted_when_response_uses_tool_calls` 继续通过。验证：native venv 下 `tests/test_agent_loop.py tests/test_llm_client.py tests/test_chat_ws.py tests/test_agent_loop_task_creation.py tests/test_tool_preflight.py tests/test_tools_schema.py tests/test_langgraph_rag_runtime.py` 为 `90 passed, 1 warning`；前端 `npm.cmd test -- src/stores/chatStore.test.ts src/pages/ChatPage.test.tsx` 为 `61 passed, 2 skipped`，`npm.cmd run typecheck` 和 `npm.cmd run build` 均通过。已重启 `127.0.0.1:8001`，显式使用 `SP_AGENT_RUNTIME=langgraph`；真实 WebSocket 临时账号 smoke“改革开放是什么时候的事情了”事件为 `tool_call(rag_retrieve_study_materials) -> tool_result -> text_delta... -> text`，首个 `text_delta` 约 3.8s 到达，最终 `text` 约 7.6s 到达，`error=None`，说明 RAG 仍在 Agent Loop 内且后端到前端链路已真实推流。剩余观察：同一次 live 输出尾部仍偶发出现“如需了解...”式可选追问，虽然 prompt/hint 已禁止；这属于下一步 RAG 回复风格收紧，不应混入本次流式修复。

2026-06-14 追加更新：修复 LangGraph/RAG 知识问答在真实聊天里显示“聊天暂时不可用”的问题。截图复现路径是“改革开放是什么时候的事情了”：运行中的 5173/8001 实际来自隔离 worktree `D:\tmp\student-planner-langgraph-final`，链路先成功发出 `rag_retrieve_study_materials` 和 Chroma/DashScope 检索结果，但最终自然语言回答阶段失败。根因有两层：其一，当前 `.env` 的应用 LLM key 仍是占位值，RAG embedding key 可用但 `create_llm_client()` 没有复用它做回答 fallback，导致 `openai.AuthenticationError`；其二，改用 DashScope chat fallback 后，原始 RAG context 把整段维基 chunk 送给模型，简单时间问题也带上无关敏感尾部，触发 `data_inspection_failed`。本轮修复：`llm_client` 在主 LLM key 为占位值时复用 `SP_RAG_EMBEDDING_API_KEY + qwen-plus` 生成最终回答；`build_rag_context` 只把与 query 命中的短句摘录放进 runtime hint，不再把整段 chunk 原样塞给回答模型；WebSocket 错误分类补上 `AuthenticationError`、`PermissionDeniedError`、`BadRequestError/data_inspection_failed`，避免配置/模型服务问题退成泛化黄色错误；RAG runtime hint 进一步禁止结尾可选服务式追问。验证：native venv 下 `tests/test_llm_client.py tests/test_chat_ws.py tests/test_agent_loop_task_creation.py tests/test_tool_preflight.py tests/test_tools_schema.py tests/test_langgraph_rag_runtime.py` 为 `69 passed, 1 warning`；已重启 `127.0.0.1:8001` 并显式以 `SP_AGENT_RUNTIME=langgraph` 启动，真实 WebSocket 临时账号复测该问题，事件顺序为 `tool_call(rag_retrieve_study_materials) -> tool_result -> text_delta... -> text -> done`，`error=None`，最终回答正确给出 1978 年 12 月 18 日/十一届三中全会。

2026-06-14 追加更新：修复 LangGraph/RAG 聊天回答层质量不稳定的问题。用户实测发现部分复习问答被包成黄色 `需要确认` 卡片，且机器学习答疑里出现原始 `<br>`、Markdown 表格线和 emoji 堆叠。根因不是 Chroma/RAG 召回，而是 `app/agent/loop.py` 把“复习”本身当成任务写入上下文，导致纯知识问答也可能被强制 `tool_choice=required` 并误选 `ask_user`；同时 `ask_user` 描述和普通回复格式规则过宽。现已移除“复习”单词级写入触发，改用“复习计划/学习任务/写入日程/安排/创建/修改”等明确动作判断；`ask_user` 限定为写入/修改/删除数据前的阻塞式确认或必要缺参追问；RAG runtime hint 明确支持复习问答、考点解释和简答题作答，并要求纯问答直接回答、上下文不足时说明不足；普通回复规则禁止原始 `<br>`、装饰性 emoji 和可选服务式追问。新增回归覆盖纯复习问答不再触发 task tool/ask_user、RAG hint 包含“复习问答”、以及 `ask_user` schema 不用于 pure informational Q&A。验证：native venv 下 `tests/test_langgraph_rag_runtime.py tests/test_tools_schema.py` 为 `27 passed`；核心组合 `tests/test_chat_ws.py tests/test_agent_loop_task_creation.py tests/test_tool_preflight.py tests/test_tools_schema.py tests/test_langgraph_rag_runtime.py` 为 `61 passed, 1 warning`。重启 `127.0.0.1:8001` 后用真实 WebSocket 临时账号 smoke“机器学习中欠拟合和过拟合分别是什么意思”，事件包含 `rag_retrieve_study_materials`，`has_ask_user=false`，`contains_raw_br=false`。

2026-06-14 追加更新：按用户要求把 RAG 公开语料扩到更接近真实规模并重测质量。`scripts/import_public_rag_sources.py` 已支持 `--preset core|expanded`、`--limit`、`--delay`、`--skip-existing`，默认扩展集覆盖中国近现代史、世界现代史、思想政治理论和机器学习基础等主题；本轮用中文维基百科 `variant=zh-cn` 请求 80 个来源，实际导入 74 个，`data/rag/public` 公开语料约 2.1MB，失败的 6 个来源已记录在 `PUBLIC_SOURCES.json`。补充了 `机器学习报告要求.md` 的数据预处理、特征工程、模型训练、误差分析等课程报告内容，避免大语料下百科背景知识压过本地课程要求。当前 RAG 语料为 `79 documents / 2472 chunks`，仍使用 `chunk_size=520 / chunk_overlap=150`。同时修复 Chroma 旧 `corpus_key` metadata 被误判为缓存命中的问题：Chroma 现在只把 metadata 中 `corpus_key` 与当前语料一致的 chunk 视为 hit，并新增回归 `test_chroma_vector_store_refreshes_stale_corpus_metadata`；`tests/test_langgraph_rag_runtime.py` 在 native venv 下为 `14 passed`。评测脚本新增 `--quiet` 和 `--summary-output`，一次运行可同时生成汇总、300 条明细和 Markdown 样例。最终真实百炼 + Chroma 大语料评测：`total_queries=300`、`source_recall_at_k=1.0`、`top1_source_hit_rate=0.9333`、`strict_pass_rate=0.92`、`chunk_count=2472`、`embedding_provider=dashscope / text-embedding-v4`、`vector_store_effective_provider=chroma / fallback_reason=""`，首次构建 `hits=0 / misses=2472`，随后清进程缓存复查 `hits=2472 / misses=0`。报告已刷新到 `student-planner/data/rag/RAG_QUALITY_300.json`、`RAG_QUALITY_300_DETAILS.json` 和 `RAG_QUALITY_SAMPLE_QA.md`。

2026-06-13 追加更新：按用户建议改用系统原生 Python，而不是 Anaconda Python，Chroma 已真实跑通。已在 `student-planner/.venv-native` 用 `C:\Users\Chen\AppData\Local\Programs\Python\Python312\python.exe` 创建原生 Python 3.12 venv，安装 `chromadb 1.5.9` 后最小 `PersistentClient -> collection.upsert -> query` 验证通过；补齐项目依赖后，`pip check` 为 `No broken requirements found`，`bcrypt=4.3.0`。用 native venv 清空并重建 `data/rag/chroma` 后，真实百炼构建结果为 `vector_store_provider=chroma / requested=chroma / effective=chroma / fallback_reason=""`；第一次构建 `hits=0 / misses=304`，第二次清进程缓存后 `hits=304 / misses=0`。300 条 RAG 评测已用真 Chroma 刷新，结果仍为 `Recall@5=100%`、`Top1 source hit=97.67%`、严格术语通过率 `94.33%`，且 `ranking_ms` 从 SQLite fallback 约 `4.5s` 降到 Chroma 约 `0.47-0.69s`。定向回归使用 native venv 运行 `tests/test_langgraph_rag_runtime.py` 为 `13 passed`。后续本 worktree 的 LangGraph/RAG 期末版建议使用 `.venv-native\Scripts\python.exe`，不要再用 Anaconda Python 跑 Chroma。

2026-06-13 追加更新：按用户要求安装并接入 Chroma 向量库。`pyproject.toml` 已加入 `chromadb>=1.0,<1.6`，默认配置改为 `SP_RAG_VECTOR_STORE_PROVIDER=chroma`、`SP_RAG_VECTOR_STORE_DIR=data/rag/chroma`；`app/agent/rag.py` 新增 `ChromaVectorStore`，使用 Chroma `PersistentClient` 持久化 chunk 文本、metadata 和 embedding，并在 RAG/tool_result 中暴露 `vector_store_requested_provider`、`vector_store_effective_provider`、`vector_store_fallback_reason`、`vector_store_hits/misses`。本机 Windows/Anaconda 环境实测 `chromadb` 1.5.9、1.4.1、1.0.21 都会在 `collection.upsert()` 触发 native access violation，退出码 `3221225477`；0.4/0.5 系列又因 Python 3.12 缺少 `chroma-hnswlib` wheel 且本机没有 MSVC Build Tools，无法安装。因此实现了 subprocess runtime probe：Chroma 可用时直接走 Chroma；当前本机不可用时自动退到 SQLite 持久化 fallback，路径为 `data/rag/chroma/rag_vectors.sqlite3`，确保不会因为 Chroma native 崩溃让主进程退出，也不会重复消耗文档 embedding。真实百炼验证：第一次构建 `requested=chroma / effective=sqlite / hits=0 / misses=304`，第二次清进程缓存后 `hits=304 / misses=0`；300 条 RAG 评测结果仍为 `Recall@5=100%`、`Top1 source hit=97.67%`、严格术语通过率 `94.33%`，报告已刷新到 `RAG_QUALITY_300.json`、`RAG_QUALITY_300_DETAILS.json` 和 `RAG_QUALITY_SAMPLE_QA.md`。定向回归：`tests/test_langgraph_rag_runtime.py` 为 `13 passed`。

2026-06-13 追加更新：修复 RAG 只用进程内内存向量导致重启后重复消耗 embedding 的问题。当前没有引入 Chroma/FAISS，而是先落地零额外依赖的 SQLite 持久化向量库：`app/agent/rag.py` 会把 chunk embedding 存入 `student-planner/data/rag/vector_store/rag_vectors.sqlite3`，配置项为 `SP_RAG_VECTOR_STORE_PROVIDER=sqlite`、`SP_RAG_VECTOR_STORE_DIR=data/rag/vector_store`。真实百炼验证：第一次构建 `vector_store_hits=0 / vector_store_misses=304`，写入全部 304 个 chunk；清空进程缓存后第二次构建 `vector_store_hits=304 / vector_store_misses=0`，不再重新 embed 全库。新增回归 `test_sqlite_vector_store_reuses_document_embeddings`，`tests/test_langgraph_rag_runtime.py` 为 `11 passed`。注意：用户每次新 query 仍需要一次 query embedding，这是语义检索正常成本；已避免的是每次启动/评测重复 embed 文档库。

2026-06-13 追加更新：按用户要求补充真正的 RAG 最终 `result`。新增 `scripts/generate_rag_result_samples.py`，流程是 `query -> build_rag_context(top_k=5) -> LLM answer`，每个场景抽 1 条，共 6 条最终回答样例。由于当前 `SP_LLM_API_KEY` 是无效占位 key，脚本会在应用 LLM key 不可用时使用已配置的百炼 Key 调用 DashScope OpenAI-compatible `qwen-plus` 生成答案。输出文件：`student-planner/data/rag/RAG_RESULT_SAMPLE_ANSWERS.md` 和 `student-planner/data/rag/RAG_RESULT_SAMPLE_ANSWERS.json`。这份文件展示的是最终用户可见回答，区别于 `RAG_QUALITY_SAMPLE_QA.md` 的召回证据视图。

2026-06-13 追加更新：按用户要求补充“具体问答情况”查看材料。`scripts/evaluate_rag_quality.py` 现在支持 `--include-details`，会为 300 条 query 输出 top5 召回来源、分数和片段摘录；同时支持 `--sample-output` 生成 Markdown 样例。已生成两个新文件：`student-planner/data/rag/RAG_QUALITY_300_DETAILS.json`（完整 300 条明细，约 1.36MB）和 `student-planner/data/rag/RAG_QUALITY_SAMPLE_QA.md`（每个 scenario 抽 4 条可读样例，约 54KB）。注意：这些是 RAG 召回证据视图，不是再调用 LLM 生成的最终自然语言答案；用于判断“问题问出来后，RAG 实际召回了什么资料、分数是多少、能否支撑回答”。本轮还修复了一个评测产物污染语料的问题：`app/agent/rag.py` 现在会跳过所有 `RAG_*` 文件，避免质量报告和 result 样例被当作 RAG 正文加载。验证：`tests/test_langgraph_rag_runtime.py` 为 `10 passed`。

2026-06-13 追加更新：按用户要求把 RAG 质量评测从少量 smoke 扩展为规模化召回套件。`scripts/evaluate_rag_quality.py` 现在默认支持每个 scenario 50 条 query，本轮覆盖 6 个 scenario 共 300 条：五四运动、全过程人民民主、冷战格局、马歇尔计划、宪法与国家机构、机器学习课程报告。评测方式为 10 个考点 seed × 5 种提问模板，批量调用百炼 `text-embedding-v4` 生成 query embedding，统计 `Recall@5`、Top1 相关来源命中率和严格术语通过率；正式报告写入 `student-planner/data/rag/RAG_QUALITY_300.json`。启用 SQLite 持久化向量库后的最终真实百炼结果：`total_queries=300`，`source_recall_at_k=1.0`，`top1_source_hit_rate=0.9767`，`strict_pass_rate=0.9433`，`chunk_count=304`，`build_ms≈0.5s`，`query_embedding_ms≈9.9s`，`ranking_ms≈4.6s`，`vector_store_hits=304 / vector_store_misses=0`，`embedding_provider=dashscope / embedding_model=text-embedding-v4 / embedding_fallback_reason=""`。严格术语未满分主要集中在五四运动、宪法与国家机构的若干子问题：来源召回正确，但 top-k 片段不一定同时包含评测设定的 2 个以上关键术语。

2026-06-13 追加更新：公开 RAG 语料已统一为简体中文。`scripts/import_public_rag_sources.py` 现在请求中文维基百科 API 时显式使用 `variant=zh-cn`，并用简体请求标题生成 Markdown 标题和文件名；旧的繁体文件名版本已删除，新文件为 `维基百科-欧洲一体化.md`。已扫过 `data/rag/public/`、根 README、`student-planner/README.md` 和本文件，未发现常见繁体残留。验证：`tests/test_langgraph_rag_runtime.py` 为 `10 passed`；`scripts/evaluate_rag_quality.py --top-k 5` 真实百炼结果仍为 `6/6 passed`，`chunk_count=304`，向量构建约 `15.5s`，单条查询约 `172-235ms`。

2026-06-13 追加更新：按用户要求继续扩大 RAG 公开语料。新增 `scripts/import_public_rag_sources.py`，通过中文维基百科 MediaWiki `extracts` API 导入公开条目，输出到 `student-planner/data/rag/public/`，并在 `PUBLIC_SOURCES.json` 记录来源、URL、许可和错误。当前已导入 10 份 CC BY-SA 4.0 公开资料：五四运动、辛亥革命、中国抗日战争、国共内战、鸦片战争、冷战、马歇尔计划、欧洲一体化、中华人民共和国宪法、全过程人民民主；`共同富裕` 两次请求均因 HTTP 429 未导入，暂不阻塞。语料库现在共 15 个 Markdown/资料文件，真实百炼 `text-embedding-v4` 评测切出 `chunk_count=304`，`chunk_size=520 / chunk_overlap=150`。验证：`tests/test_langgraph_rag_runtime.py` 为 `10 passed`；核心回归 `tests/test_chat_ws.py tests/test_agent_loop_task_creation.py tests/test_tool_preflight.py tests/test_tools_schema.py tests/test_langgraph_rag_runtime.py` 为 `54 passed`；`scripts/evaluate_rag_quality.py --top-k 5` 真实百炼结果为 `6/6 passed`，`embedding_provider=dashscope / embedding_model=text-embedding-v4 / embedding_fallback_reason=""`，向量构建约 `15.5s`，单条查询约 `172-235ms`，用例覆盖五四运动、全过程人民民主、冷战、马歇尔计划、中华人民共和国宪法和机器学习报告。

2026-06-13 追加更新：按用户要求扩大 RAG 语料并把 chunk overlap 从 80 调整到 150。当前 `app/agent/rag.py` 使用 `RAG_CHUNK_SIZE=520 / RAG_CHUNK_OVERLAP=150`，并新增 retriever 缓存，避免长文档场景每次查询都重复给全部 chunk 调用百炼 embedding。语料库新增 3 份较长测试资料：`中国近现代史复习大纲.md`、`思想政治理论复习大纲.md`、`世界现代史专题案例.md`，连同原有大学英语和机器学习资料共切出 `chunk_count=26`。真实百炼长语料评测先暴露一次 `BadRequestError`：百炼 `text-embedding-v4` 单次 batch 不能超过 10 条 input；现已把 `OpenAICompatibleEmbeddings` 改为每批最多 10 条，并新增 `SP_RAG_EMBEDDING_BATCH_SIZE=10`。验证：`tests/test_langgraph_rag_runtime.py` 为 `10 passed`；`scripts/evaluate_rag_quality.py --top-k 5` 真实百炼运行结果为 `5/5 passed`，`embedding_provider=dashscope / embedding_model=text-embedding-v4 / embedding_fallback_reason=""`，向量构建约 `7.6s`，缓存后单条查询约 `166-218ms`，五个用例覆盖中国近现代史、政治理论、冷战、欧洲一体化和机器学习报告。

2026-06-13 追加更新：LangGraph/RAG 期末版已把向量层从纯本地 `HashEmbeddings` 升级为“阿里云百炼 DashScope Embedding + 本地 fallback”。新增配置项 `SP_RAG_EMBEDDING_PROVIDER=dashscope`、`SP_RAG_EMBEDDING_MODEL=text-embedding-v4`、`SP_RAG_EMBEDDING_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1`、`SP_RAG_EMBEDDING_API_KEY`；`app/agent/rag.py` 通过 OpenAI-compatible Embeddings API 调用百炼，未配置 Key 或请求异常时退回本地 hash embedding，并在 RAG `tool_result` 中暴露 `embedding_provider`、`embedding_model`、`embedding_fallback_reason`，方便答辩说明实际运行路径。README 与 `.env.example` 已同步；自动化测试已显式隔离真实 Key，避免默认测试依赖外网。验证：`C:\Users\Chen\anaconda3\python.exe -m pytest -q tests/test_langgraph_rag_runtime.py` 为 `9 passed`；核心回归 `tests/test_chat_ws.py tests/test_agent_loop_task_creation.py tests/test_tool_preflight.py tests/test_tools_schema.py tests/test_langgraph_rag_runtime.py` 串行提升权限运行后为 `53 passed`；用户填入百炼 Key 后，真实 smoke 返回 `key_present=True / embedding_provider=dashscope / embedding_model=text-embedding-v4 / fallback_reason=none / hit_count=2`。

2026-06-13 追加更新：已在隔离 worktree `D:\tmp\student-planner-langgraph-final` 的 `codex/langgraph-rag-submission` 分支完成期末作业版 LangChain / LangGraph / RAG 接入。实现策略是“框架外层 + 稳定业务内核”：`SP_AGENT_RUNTIME=langgraph` 时，聊天入口先经过 `app/agent/langgraph_loop.py` 的 `StateGraph(load_context -> retrieve_study_materials -> compose_runtime_hints)`，再把 RAG 上下文交给原有 `run_agent_loop`；工具 schema 通过 `app/agent/langchain_tools.py` 暴露给 LangChain，RAG 资料位于 `student-planner/data/rag/`。本轮修复了 LangGraph wrapper 初版用 `async for` 代理旧 loop 时丢失 `.asend(answer)` 的关键问题，确保 `ask_user` 确认答案能传回旧 Agent；同时增加 `review_override` orphan fallback，避免确认卡在重连/竞态后丢失 pending generator。验证：安装真实 `langchain/langgraph/langchain-openai/langchain-text-splitters` 后，后端核心回归 `tests/test_chat_ws.py tests/test_agent_loop_task_creation.py tests/test_tool_preflight.py tests/test_tools_schema.py tests/test_langgraph_rag_runtime.py` 为 `51 passed`；前端 `npm.cmd run typecheck` PASS，`npm.cmd run build` PASS；浏览器 RAG smoke `1 passed`，证据为 `output/playwright/agent-loop-e2e-langgraph-rag-smoke-emits-LangGraph-RAG-retrieval-events-before-model-delegation.json`，显示 `rag_retrieve_study_materials`、`uses_langgraph=true`、`uses_langchain_tools=true`；真实模型 + 浏览器复习计划写入 E2E `1 passed (57.2s)`，证据为 `output/playwright/agent-loop-e2e-study-plan-confirmed-write-generates-a-study-plan-and-writes-confirmed-review-tasks.json`，`plan_write.created_count=19 / failed_count=0`。根 README 与 `student-planner/README.md` 已同步期末版架构、运行开关和验证命令。

2026-06-14 追加更新：完成 Chat RAG/长期记忆回复前后端契约对齐。根因是 `recall_memory` 后的最终回复仍只通过普通 `text/text_delta` 事件发送，前端没有 `answer_kind/grounding` 等结构化字段可用，只能把 RAG 回复当普通 Markdown 气泡渲染，回复风格和来源展示都交给模型自由发挥。本轮后端在 `recall_memory` 参与后为流式 delta 与最终 text 补 `answer_kind="rag"` 和 `grounding={kind:"memory", label:"基于长期记忆", items:[...]}`；prompt 同步要求记忆型回答短段落、不输出 memory id/JSON/完整来源清单。前端 `chatStore` 已保存并合并 RAG 元数据，`ChatPage` 新增固定的长期记忆来源区域，避免模型自己组织来源格式导致风格乱套。验证：后端窄回归 `py -3.12 -m pytest -q tests/test_agent_loop.py::test_simple_text_response tests/test_agent_loop.py::test_tool_call_then_text tests/test_agent_loop.py::test_streamed_text_emits_delta_before_final_text tests/test_agent_loop.py::test_recall_memory_text_response_includes_rag_metadata` 为 `4 passed`；前端 `npm.cmd test -- src/stores/chatStore.test.ts src/pages/ChatPage.test.tsx` 为 `63 passed, 2 skipped`；`npm.cmd run typecheck` PASS；`npm.cmd run build` PASS。
2026-06-07 追加更新：按远端 README 展示诉求，根目录 `README.md` 的两张体验快照已从 Markdown 图片语法改为显式 HTML `<img>` 内嵌展示，并设置 `width="100%"`，确保 GitHub README 页面直接渲染图片而不是呈现为普通文件链接。
2026-06-07 追加更新：根目录 `README.md` 已加入最新体验快照区块，引用两张仓库内图片资产：`docs/assets/readme/agent-loop-full-ui-pages.png`（21 张移动端全页面截图汇总）和 `docs/assets/readme/agent-loop-live-e2e.png`（11 条 Agent Loop live E2E 证据汇总）。这些图片由 `student-planner/frontend/output/playwright/agent-loop-live-full-ui/` 的最新 contact sheet 复制而来，用于远端 README 展示。
2026-06-07 追加更新：完成 Agent Loop live E2E + 全页面移动端截图收口。本轮把截图统一集中到 `student-planner/frontend/output/playwright/agent-loop-live-full-ui/`：全页面移动端截图 21 张位于 `pages/`，汇总图为 `pages-contact-sheet.png`；Agent Loop live E2E 最新证据 11 组 PNG + 11 组 JSON 位于 `live-e2e/`，汇总图为 `live-e2e-contact-sheet.png`。页面指标复核：`screenshots=21 / horizontalProblemScreens=0 / nonPageClippingScreens=0`。修复项：`/me`、课程、偏好、通知等移动端长内容页不再被内部卡片裁切；长用户名可换行；隐藏文件输入不再造成误报宽度；live E2E 截图等待最终 UI idle；WebSocket 发送现在会等待后端认证完成的 `connected/session_id`，首轮重连退避降到 250ms，避免上一轮连续操作中第二条消息落到未认证的新连接；E2E harness 在确认后短等 DB/事件收敛，避免确认卡被二次点击。验证：`npm.cmd test -- src/pages/ChatPage.test.tsx src/stores/chatStore.test.ts` 为 `61 passed, 2 skipped`；`npm.cmd run typecheck` PASS；`npm.cmd run build` PASS；沙箱外真实 provider `npm.cmd run e2e:agent-loop -- --reporter=list --global-timeout=900000` 为 `11 passed (4.9m)`。
2026-06-07 追加更新：完成 Chat V10 收尾体验。后端已把计划确认写入结果新增为结构化 `result` 事件（`data.kind=plan_write`），同时保留同 `message_id` 的 `text` 事件兼容旧展示；前端不再靠关键词识别写入成功/失败，而是直接渲染结构化结果卡。确认卡现在支持单条任务局部编辑/删除，确认时前端会发送 `review_override=<json>`，但界面只显示“已选择：确认”，不会把内部 payload 暴露给用户。Prompt 层新增普通回复格式约束，要求日常回答短段落、少表格/长 Markdown/符号堆叠。另修复移动端长确认卡撑高整页的问题：`.app-frame` 固定为 `100svh/100dvh` 并隐藏外层溢出，滚动回到 `.page` / `.message-list` 内部，输入栏和底部导航保持在视口内；任务编辑里的日期独占一行、开始/结束两列排布，避免 390px 小屏截断。真实 WebSocket + 模型 + 工具链 live 回归已补：`npm.cmd run e2e:agent-loop -- --grep "generates a study plan and writes confirmed review tasks" --reporter=list --global-timeout=420000` 沙箱外为 `1 passed (58.6s)`，evidence 显示 29 个 WebSocket 事件、`result/plan_write` 创建 4 条/失败 0 条。截图证据已集中到 `student-planner/frontend/output/playwright/chat-v10-live-and-mobile/`：`live-study-plan-confirmed-write.png/json`、`mobile-long-conversation-input.png`、`mobile-review-card-bottom.png`、`metrics.json`。移动端 390x640 指标：长对话 `inputClearOfNav=true / listAtBottom=true`；确认卡底部 `actionsClearOfInput=true`，确认按钮 bottom=450、输入栏 top=476。验证：前端 Chat 定向 `60 passed, 2 skipped`；后端 Agent/WS 相关 `33 passed, 1 warning`（既有 `.pytest_cache` 权限）；`npm.cmd run typecheck` PASS；`npm.cmd run build` PASS。
2026-06-07 追加更新：完成 Frontend full experience V9（mock API 移动端完整体验巡检）。本轮覆盖 `/chat`、`/calendar` 日视图/月视图/新增任务弹层、`/me/courses`、`/me/preferences`、`/me/notifications` 和 `/me`，截图集中到 `student-planner/frontend/output/playwright/full-experience-v9/`；发现并修复 Calendar 新增任务底部面板在 390px 小屏下高度吃满、容易挤压底部操作区的问题，`.task-sheet__panel` 现在限制为 `max-height: calc(100svh - 18px)` 并允许内部滚动。修复后用 390x640 视口复核，截图为 `student-planner/frontend/output/playwright/full-experience-v9-after/calendar-add-task-sheet-fixed.png`，面板指标为 `bottom=640 / maxHeight=622px / overflowY=auto`。验证：`npm.cmd test -- src/pages/CalendarPage.test.tsx` 为 `8 passed`；`npm.cmd run typecheck` PASS；`npm.cmd run build` PASS。本轮未覆盖真实 WebSocket + 模型 + 工具调用 live 闭环，仍保留在后续待优化项。
2026-06-07 追加更新：完成 Chat 执行阶段状态 V8。前端现在会把已有 `tool_call/tool_result/ask_user` 事件映射成用户可理解的阶段轨迹，例如“理解需求 / 查询可用时间 / 生成复习计划 / 写入日程任务 / 等待确认 / 整理结果”，不展示真实 chain-of-thought；确认卡点击后等待态也会显示“确认已收到 -> 写入日程任务/更新课表信息 -> 整理结果”。同时补齐 `create_work_plan`、课程增删改、课表导入等工具的前端进度标签。验证：`npm.cmd test -- src/pages/ChatPage.test.tsx` 沙箱外为 `43 passed, 2 skipped`（普通沙箱仍可能因 Vitest setup 绝对路径报 `/@fs/D:/.../src/test/setup.ts` 找不到）；`npm.cmd run typecheck` PASS；`npm.cmd run build` PASS。Playwright 本地视觉烟测截图位于 `student-planner/frontend/output/playwright/chat-ui-v8-thinking-stages.png`。
2026-06-07 追加更新：完成 Chat 消息系统体验收口 V7。前端现在在进入对话后会把大面积首屏工作台收起为紧凑状态条；工具思考中、工具进度、计划确认、课表确认、课程维护确认、写入成功/部分失败和普通 Markdown 回复分别有独立展示形态。长计划默认展示前 3 条任务并用“展开剩余 N 条任务”收纳，课表默认展示前 4 门课程并可展开剩余；最终写入成功、自动重排、部分失败文案会被渲染为结果卡，而不是普通文字气泡。验证：`npm.cmd test -- src/pages/ChatPage.test.tsx` 为 `42 passed, 2 skipped`；`npm.cmd run typecheck` PASS；`npm.cmd run build` PASS。截图位于 `student-planner/frontend/output/playwright/chat-ui-v7-*.png`。
2026-06-07 已收口打包：Chat V8 后续 5 项已在 Chat V10 完成：结构化写入结果事件、真实 WebSocket live 视觉回归、确认卡单条任务编辑/删除、prompt 普通回复格式约束、移动端长对话/底部输入栏/确认按钮复核。

## 当前阶段
2026-05-31 更新：当前活跃工作临时切到 Plan 9「Agent Execution Loop V1」，目标是先把 Agent 从“能说会做的 MVP”推进到“工具执行闭环可验证”的 V1。Plan 9 已完成后端核心实现与回归：课程有效周统一判断、课表离散单双周解析、课程提醒按真实有效周创建、`create_task`/`update_task` 支持 task reminder 一体化同步、Agent 多轮确认后修改任务时间与提醒的回归用例已覆盖。定向测试命令：`C:\Users\Chen\anaconda3\python.exe -m pytest -q tests/test_schedule_parser.py tests/test_course_occurrence.py tests/test_tool_executor.py tests/test_set_reminder_scheduling.py tests/test_auto_reminder.py tests/test_bulk_import.py tests/test_agent_loop_task_creation.py tests/test_tools_schema.py`，结果 `42 passed`；后端全量命令：`C:\Users\Chen\anaconda3\python.exe -m pytest -q`，结果 `214 passed`。
2026-05-31 追加更新：Plan 9 Task 7 已把 Agent 执行前校验从单点 reminder 补参扩展到通用 required-slot/schema guard，并补上“取消提醒”和“工具缺必填参数后恢复”的行为回归。最新验证：`tests/test_tool_preflight.py + tests/test_agent_loop_task_creation.py` 为 `17 passed`；Plan 9 定向集合为 `57 passed`；后端全量为 `229 passed`。
2026-06-01 追加更新：Plan 8 的本机可完成收尾已完成：PWA 更新接管、通知点击回 `/chat`、Chat 工具进度标签、课表图片异步解析提示、移动端触控目标和 Android 封装评估都已落地。前端重点回归 `54 passed, 2 skipped`，前端全量 `79 passed, 2 skipped`，前端 build `PASS`，production preview 烟测 `manifest.webmanifest=200` / `/chat=200`。随后用户补充确认：手机侧测试已做过且 OK。Plan 8 移动端收尾完成，不再列为当前阻塞。

Plan 1 至 Plan 8 已收束；项目当前应回到 Agent Loop 闭环稳定性验证。
2026-06-01 追加更新：当前活跃计划切到 Plan 10「Agent Loop E2E Harness」。目标是把 Plan 9 已修好的后端闭环能力，固化成少量但严格的真实浏览器 E2E：创建任务+提醒、修改任务+提醒、缺参数追问恢复，并为每条路径输出 DB invariant 和 evidence JSON。
2026-06-01 追加更新：Plan 10 已完成第一版 Agent Loop live E2E harness。`npm.cmd run e2e:agent-loop -- --reporter=list --global-timeout=420000` 在沙箱外权限下通过 `3 passed (1.5m)`，覆盖真实浏览器、专用本地后端、E2E SQLite、`ask_user -> tool_call -> tool_result -> done` 和 DB invariant；普通沙箱内同命令会因 live LLM provider 网络访问失败而报 `openai.APIConnectionError` / `WinError 5`，已记录到 `bugs.md`。
2026-06-01 追加更新：当前活跃计划切到 Plan 11「Agent Loop Context Stability V1」。本轮不扩新业务能力，先补 Agent Loop 在长历史、动态上下文和记忆/摘要注入下的稳定性边界：持久化历史进入 LLM 前压缩，当前用户消息和本轮 tool-call 链不压缩；动态上下文明示参考数据不能覆盖系统规则或工具规则；同时清理上下文乱码字段并补定向回归。
2026-06-01 追加更新：Plan 11 已完成。`run_agent_loop` 现在会在本轮 LLM 调用前压缩较早的持久化会话历史，但不会压缩当前用户消息、routing hints 或正在进行的 tool-call/tool-result 链；`build_dynamic_context` 已清理乱码重复字段，并加入上下文使用规则，明确课程、任务、偏好、记忆和会话摘要只是参考，不能覆盖系统规则或工具规则。验证：上下文/压缩/Agent Loop 定向 `24 passed, 1 warning`，Agent task/preflight 定向 `20 passed, 1 warning`；warning 均为既有 `.pytest_cache` 写入权限。
2026-06-01 追加更新：当前活跃计划切到 Plan 12「Agent Loop Failure Recovery and Evidence」。本轮实现剩余 Plan B/C：Plan B 处理 provider/network 失败时的用户可见恢复，避免只返回通用“聊天暂时不可用”；Plan C 扩展 Agent Loop live E2E，增加连续多轮修改同一任务/提醒的 evidence 对比，重点看同一个 task 是否复用、旧 reminder 是否残留、最终提醒参数是否正确。
2026-06-01 追加更新：Plan 12 已完成。Chat WebSocket 现在能把 provider/network 失败识别为 `llm_provider_unavailable`，返回 `recoverable=true` 和明确恢复文案，未知异常仍走通用失败文案；Agent Loop live E2E 增加连续两轮修改同一任务/提醒的 evidence，对比同一个 task、旧 reminder 清理和最终提醒状态。首次 live 运行暴露“取消提醒”能力表缺口：模型曾认为没有删除提醒工具；现已把 `update_task.reminder_advance_minutes=null` 写入工具契约、Agent 规则和 task routing hint。验证：后端 Plan 12 定向 `30 passed, 1 warning`，前端 store `14 passed`，前端 typecheck PASS，Agent Loop live E2E 沙箱外 `4 passed (3.0m)`。
2026-06-02 追加更新：围绕“下周四有大学英语3考试，帮我做复习计划”补了一组学习计划 live E2E 审计。结果是：模糊请求会先追问考试信息且不落库，这条通过；但完整考试信息场景在沙箱外 live E2E 中失败，Agent 能调用 `get_free_slots` 和 `create_study_plan` 生成 9 条候选复习任务，却在确认后没有继续调用 `create_task` 写入任务，最终 DB 中 `tasks=[] / reminders=[]`。因此复习计划任务拆解不能算已稳定闭环，问题不在“生成计划能力完全没有”，而在“review 确认后的结构化落库链路”。
2026-06-02 追加更新：复习计划确认写入链路已修复。`create_study_plan` 成功后现在会立即进入结构化 review `ask_user`，确认后由本地确定性分支逐条调用 `create_task` 写入日程，并记录 AgentLog；如果模型把 `available_slots` 压缩成摘要，`run_agent_loop` 会复用最近一次真实 `get_free_slots` 结果补回完整 `slots`，避免计划工具因参数摘要化失败。验证：后端定向 `35 passed, 1 warning`，前端 typecheck PASS，沙箱外 live E2E `npm.cmd run e2e:agent-loop -- --grep "study plan" --reporter=list --global-timeout=420000` 为 `2 passed (56.0s)`；普通沙箱仍可能因 provider 网络权限报 `openai.APIConnectionError` / `WinError 5`。
2026-06-02 追加更新：当前活跃计划切到 Plan 13「Study Plan Decomposition Quality」。目标是在现有学习计划确认写入闭环之上，补齐生成前的学习上下文 intake：完整考试请求如果缺少复习范围、薄弱点、目标或每日最大可学习时长，先追问一次，再把结构化 `study_context` 传给 `create_study_plan`，让写入日程的任务体现这些学习变量，而不是只按空闲时间模板化平均铺任务。设计文档：[2026-06-02-study-plan-decomposition-quality-design.md](/D:/student_time_plan/docs/superpowers/specs/2026-06-02-study-plan-decomposition-quality-design.md)，执行计划：[2026-06-02-study-plan-decomposition-quality.md](/D:/student_time_plan/docs/superpowers/plans/2026-06-02-study-plan-decomposition-quality.md)。
2026-06-02 追加更新：Plan 13 已完成。`create_study_plan` 工具 schema 现在支持 `study_context`，`tool_executor` 会把上下文传给 `generate_study_plan`；`PLAN_PROMPT` 要求使用复习范围、薄弱点、目标成绩和每日上限，并按基础覆盖、薄弱突破、综合练习、错题/考前回顾组织任务。`run_agent_loop` 在完整考试请求缺少学习上下文时会先本地发起 intake，用户可补充范围/薄弱点/目标/每日上限或回复“按默认”；随后通过 system context 和执行前 guard 双保险把上下文写入 `study_context`，确认后仍由本地确定性分支逐条 `create_task`。验证：后端学习计划/Agent/schema 定向 `20 passed, 1 warning`；后端相关回归 `40 passed, 1 warning`；前端 typecheck PASS；沙箱外 study plan live E2E `2 passed (1.2m)`。最新 evidence 显示完整请求写入 9 条大学英语3复习任务，包含 Unit1-6 基础复习、听力专项突破、写作专项突破、综合模拟、错题回顾和考前冲刺。
2026-06-02 追加更新：当前活跃计划切到 Plan 14「Task Decomposition Scenarios V1」。本轮只扩短中期高频任务拆解：作业/报告/大作业、多考试复习、计划调整、时间不足/冲突处理；长期目标拆解、学期级成长规划和完整学习诊断明确不在范围内。设计文档：[2026-06-02-task-decomposition-scenarios-v1-design.md](/D:/student_time_plan/docs/superpowers/specs/2026-06-02-task-decomposition-scenarios-v1-design.md)，执行计划：[2026-06-02-task-decomposition-scenarios-v1.md](/D:/student_time_plan/docs/superpowers/plans/2026-06-02-task-decomposition-scenarios-v1.md)。
2026-06-02 追加更新：Plan 14 已完成后端核心实现和定向回归，live E2E 仍待补。当前新增 `create_work_plan` 候选计划工具、`work_planner.py`、作业/报告计划本地 intake 与确认写入、通用计划 review/write，以及“计划太满/每天最多 N”本地调整入口；多考试复习增加后端回归。定向验证：`C:\Users\Chen\anaconda3\python.exe -m pytest -q tests/test_work_planner.py tests/test_tool_executor.py tests/test_tools_schema.py tests/test_agent_loop_task_creation.py` 为 `34 passed, 1 warning`，warning 仍是既有 `.pytest_cache` 权限。下一步补并运行 Agent Loop live E2E。
2026-06-02 追加更新：Plan 14 已完成。`create_work_plan`、作业/报告计划 shortcut、通用计划确认写入、计划压缩调整、多考试覆盖 guard 和复习计划 deterministic fallback 已落地。live E2E 新增三条：作业/报告计划写入阶段任务、多考试复习至少覆盖两门考试、已有计划压缩到每日上限；其中多考试 E2E 曾暴露模型会传压缩/不完整 slots 且漏掉高数任务，现已通过 `_has_real_available_slots` 和 `generate_study_plan` 缺科目 fallback 修复。验证：后端相关回归 `50 passed, 1 warning`；前端 typecheck PASS；沙箱外 Agent Loop live E2E 新增场景 `3 passed (1.8m)`。evidence 位于 [output/playwright](/D:/student_time_plan/output/playwright)，核心文件包括 work-plan、multi-exam 和 plan-adjustment 三个 JSON。
2026-06-03 追加更新：已收口两个复习计划体验问题：简单考试复习计划不再强制追问复习范围、薄弱点、目标成绩和每日上限，若课程和考试日期已明确，会给 `create_study_plan.study_context` 注入 `{"using_defaults": true, "raw_notes": "按默认"}` 并继续默认均衡生成；只有用户明确要求详细、精准、冲刺、定制或针对性计划且缺上下文时才追问。另补 `get_free_slots` 晚课回归：当天有 `18:00-20:00` 课程时，`20:00-22:00` 仍作为可用空闲段返回。验证：`C:\Users\Chen\anaconda3\python.exe -m pytest -q tests/test_work_planner.py tests/test_study_planner.py tests/test_agent_loop_task_creation.py tests/test_tool_preflight.py tests/test_tool_executor.py tests/test_tools_schema.py tests/test_calendar.py` 为 `58 passed, 1 warning`，warning 仍是既有 `.pytest_cache` 权限。
2026-06-03 追加更新：已完成确认写入候选计划时的主动重排 V1。现在复习计划/作业计划在 review 确认后逐条 `create_task` 写入时，如果某条因时间冲突失败，会在同一计划日期范围内重新 `get_free_slots`，选择能容纳原任务时长的空闲段重试 `create_task`；成功时最终文案标注“自动重排”，找不到空档时仍提示未写入。新增后端回归覆盖“09:00-10:00 冲突后改到 10:00-11:00 写入”和“08:00-22:00 全满时不乱写”；新增 live E2E `plan-write-active-reschedule`，通过在候选计划 review 后插入临时占用，验证原 `2026-06-03 08:00-09:00` 冲突后自动写到 `09:00-10:00`。验证：后端相关回归 `60 passed, 1 warning`；前端 typecheck PASS；沙箱外新增 live E2E `1 passed (45.9s)`。evidence：[plan-write-active-reschedule JSON](/D:/student_time_plan/output/playwright/agent-loop-e2e-plan-write-active-reschedule-reschedules-a-confirmed-plan-task-when-the-original-slot-conflicts-before-write.json)。
2026-06-04 追加更新：当前活跃计划切到 Plan 15「Course Maintenance Agent Loop V1」。本轮不继续扩任务拆解，而是补课表真值维护：课程改名、重复合并和错误删除都应先 `list_courses`，再 review 确认，最后确定性调用 `update_course` / `delete_course` 落库。设计文档：[2026-06-04-course-maintenance-agent-loop-v1-design.md](/D:/student_time_plan/docs/superpowers/specs/2026-06-04-course-maintenance-agent-loop-v1-design.md)，执行计划：[2026-06-04-course-maintenance-agent-loop-v1.md](/D:/student_time_plan/docs/superpowers/plans/2026-06-04-course-maintenance-agent-loop-v1.md)。
2026-06-04 追加更新：Plan 15 已完成。课程维护 shortcut 现在覆盖课程改名、错误课程删除、重复课程合并；都会先 `list_courses`，进入 review 确认，再确定性执行 `update_course` / `delete_course`。找不到目标时只提示不写库；同名多条删除且用户没给周几/时间/地点时提示补充，避免误删。Agent 规则和 system prompt 已同步要求“不要重新要求上传课表”。新增 live E2E `course-maintenance-rename`，验证真实浏览器中把 `机器人程序动化` 改成 `机器人流程自动化` 的链路为 `list_courses -> ask_user -> update_course -> done`，DB 最终只保留改名后的课程。验证：课程维护定向 `7 passed, 1 warning`；Agent/tool 相关回归 `66 passed, 1 warning`；前端 typecheck PASS；沙箱外新增 live E2E `1 passed (13.8s)`。evidence：[course-maintenance-rename JSON](/D:/student_time_plan/output/playwright/agent-loop-e2e-course-maintenance-rename-renames-an-imported-course-after-confirmation.json)。
2026-06-05 追加更新：本轮完成 Frontend Experience V3 原型，不接新后端接口，视觉方向从“原框架换皮”调整为更接近 ChatGPT / Claude 移动端 App 的精致 AI 工具界面。主要改动在 `frontend/src/index.css`、`AppShell.tsx` 和各页面组件：全局改为轻灰背景、极简顶部栏、浮动聊天输入框、克制底部导航和黑白主按钮；Chat 首屏改成 AI App 空状态与可点击建议入口；Calendar 改成大日期、周条、轻量 agenda 列表；Me/Courses/Preferences/Notifications 统一成原生列表和设置面板。验证：前端 typecheck PASS；重点前端测试 `55 passed, 2 skipped`；前端 build PASS。截图位于 [output/playwright](/D:/student_time_plan/output/playwright)，文件前缀为 `frontend-v3-`。
2026-06-05 追加更新：根据用户反馈，已继续重做偏好设置和通知设置两页的 V4 视觉：偏好页从单张大表单卡片改为“规划规则摘要 + 学期/空闲时间 + 课节/提醒”分组设置行；通知页从三块大状态卡改为“推送链路”步骤面板和轻量操作区。验证：前端 typecheck PASS；`NotificationsPage.test.tsx` 为 `2 passed`；重点前端测试 `55 passed, 2 skipped`；前端 build PASS。截图见 [frontend-v4-preferences.png](/D:/student_time_plan/output/playwright/frontend-v4-preferences.png) 和 [frontend-v4-notifications.png](/D:/student_time_plan/output/playwright/frontend-v4-notifications.png)。
2026-06-05 追加更新：根据用户继续反馈“页面太长，应缩进到按钮里点击展开”，已完成 Frontend Experience V5 紧凑入口版：偏好页默认只显示摘要卡和两个折叠入口，通知页默认只显示推送链路入口和操作按钮，课表页从 7 个纵向长折叠块改为 3 列星期按钮矩阵，点击某一天才在下方展开该日课程明细；二级页重复的大标题说明区已收掉，避免手机首屏被说明文案占满。验证：前端 typecheck PASS；重点前端测试 `55 passed, 2 skipped`；前端 build PASS。截图见 [frontend-v5-preferences.png](/D:/student_time_plan/output/playwright/frontend-v5-preferences.png)、[frontend-v5-notifications.png](/D:/student_time_plan/output/playwright/frontend-v5-notifications.png)、[frontend-v5-courses.png](/D:/student_time_plan/output/playwright/frontend-v5-courses.png) 和对应 `*-open.png` 展开态。
2026-06-05 追加更新：根据用户认可的非 GPT 化 Image V2 方向，已完成 Frontend Experience V6 全局视觉复刻：统一为暖白背景、墨蓝文字、青绿主色、天蓝任务色和琥珀警告色；Chat 改成学习工作台卡片 + 快捷任务入口 + 统一输入栏；Calendar 改成周条、摘要和时间轴；Courses 改成导入按钮 + 一排星期选择 + 单日课程时间轴；Preferences 保留分组设置入口但统一为原生设置列表质感；Notifications 增加推送健康度、三段状态 chip 和统一操作按钮；Me 页也同步同一套卡片、图标和底栏。验证：前端 typecheck PASS；重点前端测试 `55 passed, 2 skipped`；前端 build PASS。截图见 [frontend-v6-chat.png](/D:/student_time_plan/output/playwright/frontend-v6-chat.png)、[frontend-v6-calendar.png](/D:/student_time_plan/output/playwright/frontend-v6-calendar.png)、[frontend-v6-courses.png](/D:/student_time_plan/output/playwright/frontend-v6-courses.png)、[frontend-v6-preferences.png](/D:/student_time_plan/output/playwright/frontend-v6-preferences.png)、[frontend-v6-notifications.png](/D:/student_time_plan/output/playwright/frontend-v6-notifications.png) 和 [frontend-v6-me.png](/D:/student_time_plan/output/playwright/frontend-v6-me.png)。
2026-06-05 追加更新：已完成 Frontend Experience V6 收口复核，避免今天 V3/V4/V5/V6 多轮样式在代码里叠层混用。`frontend/src/index.css` 已从旧的多版本追加状态整理为单套 V6 主题样式；复核截图时发现并修复课表管理页课程名被竖排显示的回归；顶部在线状态也从只有圆点的空按钮收口为清晰的“在线”状态胶囊。验证：`npm.cmd run typecheck` PASS；重点前端测试 `55 passed, 2 skipped`；`npm.cmd run build` PASS。最终复核截图前缀为 `frontend-v6-final-`。
2026-06-05 追加更新：根据真实手测反馈，已修复“今晚花 3 小时复习英语六级和高数”这类明确短期复习安排被误路由成正式考试复习计划的问题。根因是后端只有考试复习/作业计划 shortcut，没有“今晚/今天晚上 + 固定总时长 + 复习科目”的本地分支，模型会把六级/高数解释成考试并追问考试日期；现在该场景会本地查询今晚空闲段，按总时长拆成复习任务，只保留写库前确认。前端同时支持 `ask_user.data.plan` 数组渲染为任务计划卡片，不再把 `plan: time/content/duration` 堆成 KV 文本。验证：后端定向 `3 passed, 1 warning`；`ChatPage.test.tsx` 为 `39 passed, 2 skipped`；前端 typecheck PASS；前端 build PASS。本地 dev server 已重启，`/health=200`、`/chat=200`。
2026-06-05 追加更新：完成同一场景真实浏览器复测与卡片微调。用临时账号在 `http://127.0.0.1:5173/chat` 发送“今晚我想花三个小时复习一下英语六级和高数，你来帮我做一下规划吧”，链路为 `get_free_slots -> ask_user(review) -> 确认 -> create_task x2 -> done`，没有追问考试日期/范围；确认卡片已结构化展示两条复习任务，确认后返回“已把 2 条复习任务写入日程。”。同时修复 `.ask-card__plan-head` 中间多一层 `div` 导致“计划任务 2”和“确认后写入日程”粘连的问题。验证：`npm.cmd run build` PASS；截图见 [tonight-review-plan-card-v2.png](/D:/student_time_plan/student-planner/frontend/output/playwright/tonight-review-plan-card-v2.png) 和 [tonight-review-after-confirm-v2-full.png](/D:/student_time_plan/student-planner/frontend/output/playwright/tonight-review-after-confirm-v2-full.png)。
2026-06-05 追加更新：完成 Chat 弹出卡片全量前端审计与体验修正。当前覆盖 17 类状态/卡片截图：thinking、工具进度 running/done、confirm、select、inline 追问、任务计划、课程维护、课表识别、KV 详情、纯文本详情、确认后等待、错误、markdown 普通回复、streaming 普通回复、附件托盘、图片解析进度；当前问题现场截图前缀为 `card-audit-current-`，修正后截图前缀为 `card-audit-final-`，均位于 [frontend/output/playwright](/D:/student_time_plan/student-planner/frontend/output/playwright)。本轮修正：assistant 普通文本支持轻量 markdown/换行渲染，避免 `###`、`**` 和列表挤成一团；“正在发送”替换为动态 thinking 卡片，并用“理解需求 / 检查日程上下文 / 整理回复格式”等高层状态轨迹展示，不暴露真实推理链；工具进度和图片解析进度增加动态光点与处理轨迹；流式 `text_delta` 消息显示光标。验证：`npm.cmd test -- src/pages/ChatPage.test.tsx` 为 `40 passed, 2 skipped`；`npm.cmd run typecheck` PASS；`npm.cmd run build` PASS。

## 最近完成
- 已完成 Frontend Experience V3 原型：未改后端接口，先用现有数据结构重做全局视觉与主要页面，方向对齐 ChatGPT / Claude 类移动端 AI App。覆盖 [AppShell.tsx](/D:/student_time_plan/student-planner/frontend/src/components/AppShell.tsx)、[ChatPage.tsx](/D:/student_time_plan/student-planner/frontend/src/pages/ChatPage.tsx)、[CalendarPage.tsx](/D:/student_time_plan/student-planner/frontend/src/pages/CalendarPage.tsx)、[MePage.tsx](/D:/student_time_plan/student-planner/frontend/src/pages/MePage.tsx)、[CoursesPage.tsx](/D:/student_time_plan/student-planner/frontend/src/pages/CoursesPage.tsx)、[PreferencesPage.tsx](/D:/student_time_plan/student-planner/frontend/src/pages/PreferencesPage.tsx)、[NotificationsPage.tsx](/D:/student_time_plan/student-planner/frontend/src/pages/NotificationsPage.tsx) 和 [index.css](/D:/student_time_plan/student-planner/frontend/src/index.css)。验证：`npm.cmd run typecheck` PASS；`npm.cmd test -- src/pages/ChatPage.test.tsx src/pages/CalendarPage.test.tsx src/pages/CoursesPage.test.tsx src/pages/NotificationsPage.test.tsx src/components/AppShell.test.tsx` 为 `55 passed, 2 skipped`；`npm.cmd run build` PASS。视觉截图见 [frontend-v3-chat.png](/D:/student_time_plan/output/playwright/frontend-v3-chat.png)、[frontend-v3-calendar.png](/D:/student_time_plan/output/playwright/frontend-v3-calendar.png)、[frontend-v3-courses.png](/D:/student_time_plan/output/playwright/frontend-v3-courses.png)、[frontend-v3-me.png](/D:/student_time_plan/output/playwright/frontend-v3-me.png)、[frontend-v3-preferences.png](/D:/student_time_plan/output/playwright/frontend-v3-preferences.png)、[frontend-v3-notifications.png](/D:/student_time_plan/output/playwright/frontend-v3-notifications.png)。
- 已完成偏好设置和通知设置 V4 修正：偏好页改为摘要 + 分组设置行，通知页改为三段推送链路步骤面板，避免继续呈现大灰表单堆叠。改动覆盖 [PreferencesPage.tsx](/D:/student_time_plan/student-planner/frontend/src/pages/PreferencesPage.tsx)、[NotificationsPage.tsx](/D:/student_time_plan/student-planner/frontend/src/pages/NotificationsPage.tsx) 和 [index.css](/D:/student_time_plan/student-planner/frontend/src/index.css)。验证：`npm.cmd run typecheck` PASS；`npm.cmd test -- src/pages/NotificationsPage.test.tsx` 为 `2 passed`；重点前端测试 `55 passed, 2 skipped`；`npm.cmd run build` PASS。截图见 [frontend-v4-preferences.png](/D:/student_time_plan/output/playwright/frontend-v4-preferences.png)、[frontend-v4-notifications.png](/D:/student_time_plan/output/playwright/frontend-v4-notifications.png)。
- 已完成 Frontend Experience V5 紧凑入口版：偏好和通知改为默认折叠入口，课表改为星期按钮矩阵 + 单日明细面板，并收掉二级页重复大标题说明区。改动覆盖 [CoursesPage.tsx](/D:/student_time_plan/student-planner/frontend/src/pages/CoursesPage.tsx)、[PreferencesPage.tsx](/D:/student_time_plan/student-planner/frontend/src/pages/PreferencesPage.tsx)、[NotificationsPage.tsx](/D:/student_time_plan/student-planner/frontend/src/pages/NotificationsPage.tsx) 和 [index.css](/D:/student_time_plan/student-planner/frontend/src/index.css)。验证：`npm.cmd run typecheck` PASS；`npm.cmd test -- src/pages/ChatPage.test.tsx src/pages/CalendarPage.test.tsx src/pages/CoursesPage.test.tsx src/pages/NotificationsPage.test.tsx src/components/AppShell.test.tsx` 为 `55 passed, 2 skipped`；`npm.cmd run build` PASS。截图见 [frontend-v5-preferences.png](/D:/student_time_plan/output/playwright/frontend-v5-preferences.png)、[frontend-v5-notifications.png](/D:/student_time_plan/output/playwright/frontend-v5-notifications.png)、[frontend-v5-courses.png](/D:/student_time_plan/output/playwright/frontend-v5-courses.png)。
- 已完成 Frontend Experience V6 全局视觉复刻：按用户认可的非 GPT 化方向统一 Chat、Calendar、Courses、Preferences、Notifications 和 Me 页，改动覆盖 [AppShell.tsx](/D:/student_time_plan/student-planner/frontend/src/components/AppShell.tsx)、[ChatPage.tsx](/D:/student_time_plan/student-planner/frontend/src/pages/ChatPage.tsx)、[CalendarPage.tsx](/D:/student_time_plan/student-planner/frontend/src/pages/CalendarPage.tsx)、[CoursesPage.tsx](/D:/student_time_plan/student-planner/frontend/src/pages/CoursesPage.tsx)、[PreferencesPage.tsx](/D:/student_time_plan/student-planner/frontend/src/pages/PreferencesPage.tsx)、[NotificationsPage.tsx](/D:/student_time_plan/student-planner/frontend/src/pages/NotificationsPage.tsx)、[MePage.tsx](/D:/student_time_plan/student-planner/frontend/src/pages/MePage.tsx) 和 [index.css](/D:/student_time_plan/student-planner/frontend/src/index.css)。验证：`npm.cmd run typecheck` PASS；重点前端测试 `55 passed, 2 skipped`；`npm.cmd run build` PASS。截图见 [frontend-v6-chat.png](/D:/student_time_plan/output/playwright/frontend-v6-chat.png)、[frontend-v6-calendar.png](/D:/student_time_plan/output/playwright/frontend-v6-calendar.png)、[frontend-v6-courses.png](/D:/student_time_plan/output/playwright/frontend-v6-courses.png)、[frontend-v6-preferences.png](/D:/student_time_plan/output/playwright/frontend-v6-preferences.png)、[frontend-v6-notifications.png](/D:/student_time_plan/output/playwright/frontend-v6-notifications.png)、[frontend-v6-me.png](/D:/student_time_plan/output/playwright/frontend-v6-me.png)。
- 已完成 Frontend Experience V6 收口复核：确认 `index.css` 不再保留 V3/V4/V5/V6 叠加式 prototype 样式，而是单套 V6 主题；修复课程页单日时间轴课程名竖排显示问题；顶部在线状态改为可读胶囊，避免右上角看起来像空按钮。验证：`npm.cmd run typecheck` PASS；`npm.cmd test -- src/pages/ChatPage.test.tsx src/pages/CalendarPage.test.tsx src/pages/CoursesPage.test.tsx src/pages/NotificationsPage.test.tsx src/components/AppShell.test.tsx` 为 `55 passed, 2 skipped`；`npm.cmd run build` PASS。最终复核截图见 [frontend-v6-final-chat.png](/D:/student_time_plan/output/playwright/frontend-v6-final-chat.png)、[frontend-v6-final-calendar.png](/D:/student_time_plan/output/playwright/frontend-v6-final-calendar.png)、[frontend-v6-final-courses.png](/D:/student_time_plan/output/playwright/frontend-v6-final-courses.png)、[frontend-v6-final-preferences.png](/D:/student_time_plan/output/playwright/frontend-v6-final-preferences.png)、[frontend-v6-final-notifications.png](/D:/student_time_plan/output/playwright/frontend-v6-final-notifications.png)、[frontend-v6-final-me.png](/D:/student_time_plan/output/playwright/frontend-v6-final-me.png)。
- 已修复短期复习安排手测暴露的问题：`今晚我想花三个小时复习一下英语六级和高数` 现在走本地短复习计划分支，不再追问考试日期/考试范围；后端会查询当天空闲时间并生成今晚复习任务确认卡。前端 [ChatPage.tsx](/D:/student_time_plan/student-planner/frontend/src/pages/ChatPage.tsx) 现在会把 `plan` / `plans` 数组识别为任务计划卡，支持 `content` 字段作为任务标题，避免出现 `plan time/content/duration` 的原始文本堆。验证：`tests/test_agent_loop_task_creation.py::test_agent_loop_handles_tonight_fixed_duration_review_without_exam_intake` 覆盖该话术；后端相关 3 条回归 `3 passed, 1 warning`；`npm.cmd test -- src/pages/ChatPage.test.tsx` 为 `39 passed, 2 skipped`；`npm.cmd run typecheck` PASS；`npm.cmd run build` PASS。
- 已完成 Plan 15「Course Maintenance Agent Loop V1」：新增 [设计 spec](/D:/student_time_plan/docs/superpowers/specs/2026-06-04-course-maintenance-agent-loop-v1-design.md) 和 [执行 plan](/D:/student_time_plan/docs/superpowers/plans/2026-06-04-course-maintenance-agent-loop-v1.md)；[loop.py](/D:/student_time_plan/student-planner/app/agent/loop.py) 现在支持课程改名、错误课程删除和重复课程合并的本地确认闭环，所有写操作都先 `list_courses` 并经过 review 确认；同名多条删除缺少周几/时间/地点时不落库。[Agent.md](/D:/student_time_plan/student-planner/Agent.md) 和 [prompt.py](/D:/student_time_plan/student-planner/app/agent/prompt.py) 已同步课程维护规则；[agent-loop.spec.ts](/D:/student_time_plan/student-planner/frontend/e2e-agent/agent-loop.spec.ts) 新增课程改名 live E2E，evidence 见 [course-maintenance-rename JSON](/D:/student_time_plan/output/playwright/agent-loop-e2e-course-maintenance-rename-renames-an-imported-course-after-confirmation.json)。验证：后端课程维护定向 `7 passed, 1 warning`，Agent/tool 相关回归 `66 passed, 1 warning`，前端 typecheck PASS，沙箱外新增 live E2E `1 passed (13.8s)`。
- 已完成确认写入候选计划时的主动重排 V1：更新 [loop.py](/D:/student_time_plan/student-planner/app/agent/loop.py)、[prompt.py](/D:/student_time_plan/student-planner/app/agent/prompt.py)、[Agent.md](/D:/student_time_plan/student-planner/Agent.md) 和 [agent-loop.spec.ts](/D:/student_time_plan/student-planner/frontend/e2e-agent/agent-loop.spec.ts)。行为边界是只处理复习/作业候选计划 review 确认后的确定性写入分支：原计划时间冲突时重新查同一计划日期范围内空闲段并重试写入；找不到空档时仍保留失败提示。验证：新增后端冲突重排回归 `2 passed`，后端相关集合 `60 passed, 1 warning`，前端 typecheck PASS，沙箱外新增 live E2E `1 passed (45.9s)`；evidence 见 [plan-write-active-reschedule JSON](/D:/student_time_plan/output/playwright/agent-loop-e2e-plan-write-active-reschedule-reschedules-a-confirmed-plan-task-when-the-original-slot-conflicts-before-write.json)。
- 已完成 2026-06-03 复习计划体验收口：更新 [loop.py](/D:/student_time_plan/student-planner/app/agent/loop.py)、[prompt.py](/D:/student_time_plan/student-planner/app/agent/prompt.py) 和 [Agent.md](/D:/student_time_plan/student-planner/Agent.md)，把学习上下文 intake 从“课程+日期但缺重点就必须追问”改成“简单请求按默认生成，详细/精准/冲刺/定制请求才追问”；新增/更新回归覆盖简单复习默认 `study_context.using_defaults`、详细复习仍出现“拆得更准”追问，以及 `18:00-20:00` 晚课后保留 `20:00-22:00` 空闲段。验证：后端相关回归 `58 passed, 1 warning`。
- 已完成 Plan 14「Task Decomposition Scenarios V1」：新增 [设计 spec](/D:/student_time_plan/docs/superpowers/specs/2026-06-02-task-decomposition-scenarios-v1-design.md) 和 [执行 plan](/D:/student_time_plan/docs/superpowers/plans/2026-06-02-task-decomposition-scenarios-v1.md)；新增 [work_planner.py](/D:/student_time_plan/student-planner/app/agent/work_planner.py) 与 `create_work_plan` 工具，用于作业、报告、大作业等 deadline 型拆解；[loop.py](/D:/student_time_plan/student-planner/app/agent/loop.py) 现在支持作业计划 intake、`get_free_slots -> create_work_plan -> review -> create_task`、通用计划确认写入，以及“计划太满/每天最多 N”确认后批量 `update_task`；[study_planner.py](/D:/student_time_plan/student-planner/app/agent/study_planner.py) 增加 deterministic fallback 和多考试缺科目补齐。验证：后端相关回归 `50 passed, 1 warning`，前端 typecheck PASS，沙箱外新增 live E2E `3 passed (1.8m)`，evidence 包含 [work-plan JSON](/D:/student_time_plan/output/playwright/agent-loop-e2e-work-plan-confirmed-write-generates-an-assignment-report-plan-and-writes-confirmed-staged-tasks.json)、[multi-exam JSON](/D:/student_time_plan/output/playwright/agent-loop-e2e-multi-exam-study-plan-generates-a-multi-exam-study-plan-with-tasks-for-both-exams.json) 和 [plan-adjustment JSON](/D:/student_time_plan/output/playwright/agent-loop-e2e-plan-adjustment-daily-limit-adjusts-an-existing-assignment-plan-to-a-lower-daily-limit.json)。
- 已完成 Plan 13「Study Plan Decomposition Quality」：新增 [设计 spec](/D:/student_time_plan/docs/superpowers/specs/2026-06-02-study-plan-decomposition-quality-design.md) 和 [执行 plan](/D:/student_time_plan/docs/superpowers/plans/2026-06-02-study-plan-decomposition-quality.md)；学习计划生成前新增本地 intake，完整考试请求如果缺少复习范围/薄弱点/目标/每日上限会先追问一次；`create_study_plan.study_context`、执行器和生成 prompt 已打通；live E2E 现在会输入“范围 Unit1-6，听力和写作薄弱，目标80分，每天最多2小时”，并断言最终 DB 任务中至少两条体现 Unit/听力/写作/薄弱/目标等上下文线索。验证：后端相关回归 `40 passed, 1 warning`，前端 typecheck PASS，沙箱外 study plan live E2E `2 passed (1.2m)`。
- 已修复学习计划拆解确认写入链路，并把 live E2E 场景固化到 [agent-loop.spec.ts](/D:/student_time_plan/student-planner/frontend/e2e-agent/agent-loop.spec.ts)：一条覆盖“下周有一门考试”这类信息不足请求，要求 Agent 先 `ask_user` 追问且不写库；一条覆盖“2026-06-11 大学英语3考试”完整请求，要求 `get_free_slots -> create_study_plan -> create_task` 并写入 2026-06-02 至 2026-06-10 范围内的复习任务。后端 [loop.py](/D:/student_time_plan/student-planner/app/agent/loop.py) 现在会在 `create_study_plan` 成功后用结构化 review 保留完整任务数据，确认后确定性逐条调用 `create_task`；同时复用最近一次真实 `get_free_slots` 结果，修复模型把 `available_slots` 摘要化导致计划工具失败的问题。验证：`tests/test_agent_loop_task_creation.py::test_agent_loop_writes_confirmed_study_plan_tasks` 为 `1 passed`；`tests/test_agent_loop_task_creation.py tests/test_tool_preflight.py tests/test_tool_executor.py tests/test_tools_schema.py` 为 `35 passed, 1 warning`；`npm.cmd run typecheck` PASS；沙箱外 `npm.cmd run e2e:agent-loop -- --grep "study plan" --reporter=list --global-timeout=420000` 为 `2 passed (56.0s)`。
- 已维护 GitHub 展示入口 README：根目录 [README.md](/D:/student_time_plan/README.md) 现在明确把 Agent 定位为 ReAct / function calling 风格的工具调用闭环，而不是严格 Plan-and-Execute；补充了 `Thought -> Action(tool_call) -> Observation(tool_result) -> Final` 的说明、`create_study_plan` 只是候选计划生成工具的边界，以及面试表述建议。子项目 [student-planner/README.md](/D:/student_time_plan/student-planner/README.md) 同步增加了实现级说明，指向 `loop.py`、`tools.py`、`tool_executor.py`、`tool_preflight.py` 和 `study_planner.py`。
- 已用用户提供的图片做了一轮安全侧检查：在本机找到的候选图片 [26535DI8P0VR242F.png](/C:/Users/Chen/Desktop/26535DI8P0VR242F.png) 实际是个人简历截图，不是课表图；由于真实 OCR 会把图片发送给外部 vision provider，本轮未在没有明确授权的情况下外发。已把 OCR prompt 收紧为“非大学课表/无周几节次网格时，不要从简历、项目经历、普通文档中提取课程”，并补上图片异步上传解析为空时的回归，确保非课表图片可以以 `PARSED + count=0` 收束，不误导成课程导入。随后继续修复 Chat shortcut 的空结果边界：空课程不再触发“共0条，确认是否导入？”确认卡，也不会追问学期开始日期/总周数，而是直接提示“没有从这张图片里识别到课程信息”。用该真实图片文件跑了不外发的本地上传探针：`image_size=3779334`，初始 `status=processing`，最终 `status=PARSED / progress=100 / count=0 / courses=[]`。验证：`tests/test_agent_loop.py::test_schedule_import_shortcut_handles_empty_image_parse_without_review_card tests/test_schedule_ocr.py tests/test_schedule_import_api.py tests/test_schedule_tools.py` 为 `34 passed, 5 warnings`，warnings 为 openpyxl deprecation 和既有 `.pytest_cache` 权限。
- 用户随后指出真正用于测试的是两张课表截图：[微信图片_20260419104519.jpg](/C:/Users/Chen/Desktop/微信图片_20260419104519.jpg) 和 [微信图片_20260419104523.jpg](/C:/Users/Chen/Desktop/微信图片_20260419104523.jpg)，上一条只看了桌面最新文件是误判。已用这两张真实文件跑不外发的本地多图上传/合并探针：第 3 周图片 `size=533142`，第 4 周图片 `size=517719`，初始 `status=processing / source_file_count=2`，最终 `status=PARSED / progress=100 / count=6`。按图中课程构造 mock OCR 结果后，后端合并得到：自然语言处理（周三 1-2，全周）、自然语言处理（周四 1-2，全周）、机器人流程自动化（周三 3-4，全周）、大模型微调技术（周三 5-6，单周）、大模型微调技术（周四 5-6，全周）、大学生就业指导（周三 9-10，全周）。真实 vision OCR 仍未跑，因为审批要求用户在说明外发风险后明确同意把这两张课表图发送给当前 vision provider。
- 用户明确要求后，已把这两张课表图发送给当前 vision provider 跑真实 OCR，并同时跑完整 `/api/schedule/upload` 异步解析链路。链路状态是通的：初始 `status=processing / source_file_count=2`，中途 `PARSING / progress=52 / count=8`，最终 `status=PARSED / progress=100 / count=13`。但 OCR 质量不合格：第 3 周图被识别成 8 条，竖排课程名拆裂；第 4 周图被识别成 5 条且部分 weekday 左移；最终合并为 13 条而人工应为 6 条。典型错误包括“机器人流程自动化”拆成“机器人程动 / 流自化”，“大学生就业指导”拆成“大生业导 / 学就指导”，第 4 周若干课程从周三/周四错成周二/周三。结论：上传/轮询/状态链路通，真实 OCR 识别质量暂不达标，已写入 `bugs.md`。
- 已完成 Plan 12「Agent Loop Failure Recovery and Evidence」：新增 [设计 spec](/D:/student_time_plan/docs/superpowers/specs/2026-06-01-agent-loop-failure-recovery-and-evidence-design.md) 和 [执行 plan](/D:/student_time_plan/docs/superpowers/plans/2026-06-01-agent-loop-failure-recovery-and-evidence.md)；Chat WebSocket provider/network 失败现在返回专用错误 code、`recoverable=true` 和用户可见恢复文案，普通未知异常保持通用失败。Agent Loop E2E 扩展为 4 条 live 场景，新增连续两轮修改同一任务/提醒的 DB invariant 和 evidence JSON，并修复 live 暴露的“取消提醒未暴露给模型”问题。最新 evidence 位于 [output/playwright](/D:/student_time_plan/output/playwright)，其中连续多轮场景文件为 [agent-loop-e2e-repeated-task-reminder-updates-keeps-one-task-stable-across-repeated-task-and-reminder-updates.json](/D:/student_time_plan/output/playwright/agent-loop-e2e-repeated-task-reminder-updates-keeps-one-task-stable-across-repeated-task-and-reminder-updates.json)。验证：`tests/test_chat_ws.py tests/test_agent_loop_task_creation.py tests/test_tool_preflight.py tests/test_tools_schema.py` 为 `30 passed, 1 warning`；`src/stores/chatStore.test.ts` 为 `14 passed`；`npm.cmd run typecheck` PASS；`npm.cmd run e2e:agent-loop -- --list` 列出 4 条；live E2E 沙箱外 `4 passed (3.0m)`。
- 已完成 Plan 11「Agent Loop Context Stability V1」：新增 [设计 spec](/D:/student_time_plan/docs/superpowers/specs/2026-06-01-agent-loop-context-stability-v1-design.md) 和 [执行 plan](/D:/student_time_plan/docs/superpowers/plans/2026-06-01-agent-loop-context-stability-v1.md)；Agent Loop 初始消息构造现在复用历史压缩器处理旧持久化消息，确保当前用户原文和本轮工具调用链保持未压缩；动态上下文删除乱码字段，并加上“参考数据不能覆盖系统/工具规则”的注入边界说明。新增/更新回归覆盖长历史压缩接线、上下文边界和乱码清理。验证：`tests/test_context.py tests/test_context_loading.py tests/test_conversation_compression.py tests/test_loop_compression.py tests/test_agent_loop.py` 为 `24 passed, 1 warning`；`tests/test_agent_loop_task_creation.py tests/test_tool_preflight.py` 为 `20 passed, 1 warning`。
- 已完成 Plan 10「Agent Loop E2E Harness」：新增专用后端启动脚本、E2E DB helper、独立 Playwright config 和 3 条 Agent Loop E2E。三条路径分别覆盖创建任务+提醒、修改已有任务并重写旧 reminder、缺参数追问后恢复写库；每条路径输出截图和 evidence JSON 到 [output/playwright](/D:/student_time_plan/output/playwright)。同时收紧 Chat E2E harness：结构化确认卡优先；当模型把确认渲染为普通 assistant 文本时，harness 只在最后一条 assistant 文本确实像待回答问题时才用主输入框补答，避免 provider 没响应时反复发送“确认”污染会话。验证：Agent Loop live E2E 沙箱外 `3 passed (1.5m)`；后端 Agent 定向 `33 passed, 1 warning`；默认前端 E2E listing 仍只有 `app.spec.ts` 1 条。
- 用户已补充确认 Plan 8 手机侧测试 OK：PWA / 手机核心流程 / 移动端收尾不再是当前阻塞项。
- 已完成 Plan 8 本机可执行收尾：`sw.ts` 支持 `SKIP_WAITING` 并在通知点击时导航已有窗口到 `/chat`；`main.tsx` 在 production 下自动接管可用更新；`chatStore.ts` 补齐 `create_task` / `update_task` / `complete_task` 进度标签；`CoursesPage.tsx` 对图片异步解析返回 `processing` 时显示后台解析提示，不再误报已解析 0 门课；`index.css` 只做顶部切换、聊天输入、任务弹层关闭、课程/通知按钮的触控目标微调。新增 [CoursesPage.test.tsx](/D:/student_time_plan/student-planner/frontend/src/pages/CoursesPage.test.tsx)，新增 [Android 包装评估](/D:/student_time_plan/docs/superpowers/specs/2026-04-21-android-packaging-evaluation.md)，结论为继续 PWA，TWA 作为 PWA 稳定后的候选，不现在引入 Capacitor。
- 已完成 Plan 9 Task 7「Agent routing and tool preflight evals」：`tool_preflight` 现在会在工具执行前检查 schema 必填参数和 enum 值，缺关键字段时不会把坏 tool call 暴露给前端或落库，而是把内部工具错误回传给 LLM 让它追问或重试；同时修复显式“不要/不提醒”时 `reminder_advance_minutes=None` 没有进入 `update_task` 的边界问题。新增回归覆盖：取消已有 task reminder、多轮更新时删除 reminder、`create_task` 缺 `scheduled_date` 被 preflight 拦截后通过 `ask_user` 恢复。验证结果：窄定向 `17 passed`，Plan 9 定向 `57 passed`，后端全量 `229 passed`。
- 已修复 2026-05-31 Agent E2E smoke 暴露的“修改任务时间时漏传提醒提前分钟”问题：新增 [tool_preflight.py](/D:/student_time_plan/student-planner/app/agent/tool_preflight.py)，在 `create_task` / `update_task` / `set_reminder` 执行前从用户已确认文本中提取低歧义提醒槽位（如“提前15分钟”“准点提醒”“不提醒”），并补齐或纠正 `reminder_advance_minutes` / `advance_minutes`；同时对“把已有任务改到/修改/调整”这类请求增加 task routing hint 和 create-task guard，防止模型把修改误走成新建。已补回归：[test_tool_preflight.py](/D:/student_time_plan/student-planner/tests/test_tool_preflight.py) 和 [test_agent_loop_task_creation.py](/D:/student_time_plan/student-planner/tests/test_agent_loop_task_creation.py)。验证结果：窄定向 `23 passed`，Plan 9 定向 `51 passed`，后端全量 `223 passed`。真实 smoke 已在 fresh backend 与当前 `5174 -> 8001` 链路通过：临时账号 `smoke_live_guard_1780231311195` 的 `linear algebra review` 从 `2026-07-26 15:00-16:00` 更新到 `2026-07-27 16:00-17:00`，`update_task` 实际参数包含 `reminder_advance_minutes=15`，最终只保留一条 `2026-07-27T15:45:00 / advance_minutes=15` reminder，无旧任务和旧 reminder 残留。当前 `8001` 后端已重启到当前源码，监听 PID 为 `41240`。
- 已完成 2026-05-31 Agent E2E smoke（子 agent + Playwright 真实浏览器）：当前本地有效入口是 `http://127.0.0.1:5174/chat`，其 `/api` 与 `/ws/chat` 指向 Student Planner 后端 `127.0.0.1:8001`。已有 `npm.cmd run e2e` 基础用例通过 `1 passed`，但只覆盖登录页。真实 Agent 创建路径已通过：临时账号输入“明天下午3点到4点提醒我复习线性代数，提前30分钟提醒”，确认后走通 `ask_user -> create_task -> set_reminder -> done`，数据库中任务与 reminder 正确落库。真实多轮修改路径暴露 bug：输入“把刚才的复习线性代数任务改到明天下午4点到5点，提前15分钟提醒”后，模型只把 `start_time/end_time` 传给 `update_task`，没有带 `reminder_advance_minutes=15`，导致任务时间已更新但旧 reminder 仍停留在 `14:30 / advance_minutes=30`。子 agent 证据文件位于 [output/playwright/smoke-b-evidence.json](/D:/student_time_plan/output/playwright/smoke-b-evidence.json)、[output/playwright/smoke-b-update-evidence.json](/D:/student_time_plan/output/playwright/smoke-b-update-evidence.json)，对应截图位于 [output/playwright/smoke-b-create-complete.png](/D:/student_time_plan/output/playwright/smoke-b-create-complete.png)、[output/playwright/smoke-b-update-complete.png](/D:/student_time_plan/output/playwright/smoke-b-update-complete.png)。
- 已修复 2026-05-01 真机通知页“看起来开启了，其实没订阅成功”的静默失败问题：后端新增 `/api/push/status` 只读状态接口，前端 [NotificationsPage.tsx](/D:/student_time_plan/student-planner/frontend/src/pages/NotificationsPage.tsx) 现在会同时显示通知权限 / 本机订阅 / 服务器订阅 / VAPID 配置状态；若浏览器里已有 `PushSubscription` 但后端未保存，会提示“重新同步”，并在点击“开启推送通知”时直接复用已有订阅回写服务端，不再盲目重复 `pushManager.subscribe()`。相关定向回归已通过：后端推送链 `12 passed`，前端通知页 `2 passed`，前端 build `PASS`，并已同步到部署机 `101.33.229.161`。
- 已定位并修复 2026-05-01 真机 PWA 提醒不弹窗的主阻塞：部署环境 `/opt/student-planner/shared/.env` 中 `SP_VAPID_PRIVATE_KEY` / `SP_VAPID_PUBLIC_KEY` 一直为空，导致 Web Push 无法完成订阅；同时数据库里 `users_with_push_subscription=0`，说明手机端此前也没有任何一次真正把订阅入库。现已在服务器生成并写入新的 VAPID 密钥、重启 `student-planner-backend`，并用本机回环 smoke 跑通了 `/api/push/vapid-key` 与 `/api/push/subscribe`。后续真机上仍需进入通知页手动点一次“开启推送通知”完成真实订阅。
- 已修复 2026-04-25 服务器课表文件导入回退到旧逻辑的问题：线上 `/opt/student-planner/current/app/agent/loop.py`、`tool_executor.py`、`services/schedule_parser.py` 与本地修复版哈希不一致，导致真机仍会复现旧的补信息提问与周次处理。现已同步这 3 个后端文件并重启 `student-planner-backend`；通过临时 HTTPS 入口做了真实 smoke：课表文件补充提问不再泄露 `missing_periods` / `missing_semester_fields`，确认卡恢复结构化 review，导入后的单周 / 双周 / 第 1 周课程都保留正确周次。
- 已修复 2026-04-25 真机 PWA 聊天页“加号无反应”问题：附件入口原先依赖 `button + ref.click()` 去触发一个 `display:none` 的文件输入，在手机/PWA 环境下不稳定。现已改为原生 `label[for=file-input]` 触发链，并保留键盘 fallback；前端全量测试 `74 passed, 2 skipped`、前端 build `PASS`。
- 已修复 2026-04-23 真机 PWA 聊天红字报错：服务器部署目录缺失 `Agent.md`，导致后端构造系统提示词时抛 `FileNotFoundError` 并返回“聊天暂时不可用，请稍后重试”。已补齐 `/opt/student-planner/current/Agent.md`，公网 WebSocket smoke 已验证 `connected`、流式文本与 `done` 正常返回。
- 已将当前版本部署到腾讯云测试机 `101.33.229.161`：后端运行在 `127.0.0.1:8000`、Nginx 反代运行在 `127.0.0.1:8080`，并通过 Cloudflare Quick Tunnel 暴露临时 HTTPS 入口 `https://browsing-gibson-cons-aggregate.trycloudflare.com`。
- 已在外部验证临时 HTTPS 环境：`/health`、`/chat`、`manifest.webmanifest` 正常返回；浏览器里 `isSecureContext=true`，`navigator.serviceWorker` 可用且已有 `1` 条注册。
- 已起本地 `0.0.0.0:8000` 后端、`0.0.0.0:5173` dev server 和 `0.0.0.0:4173` preview，并用 LAN 地址 `192.168.3.105` 验证了 `/health`、`/chat`、注册登录与普通消息收发链路。
- 已修复前端在部分浏览器环境下 `crypto.randomUUID is not a function` 会直接打断聊天发送的问题；补了 `createClientId` 兜底与对应前端回归，当前定向测试 `50 passed`、`tests/test_chat_ws.py` 为 `4 passed`、前端 build 继续 `PASS`。
- 已确认 Task 3 的核心前置：preview 入口与 manifest 正常，但 LAN 上的 `http://192.168.3.105:4173` 不是 secure context，`navigator.serviceWorker` 不可用，真机 PWA 安装需切到 HTTPS 路径。
- 已在 `codex/plan8-mobile-beta` 分支激活 Plan 8，并串行重跑本地回归基线：后端 `194 passed`、前端 `68 passed`、前端构建 `PASS`。
- 已整理 Plan 8《真机内测与体验收尾》，后续按“回归基线 -> 真机接入 -> PWA 验收 -> 核心 smoke -> 定向修补”顺序推进。
- 将项目当前目标重新明确为“真机自用内测”，不再默认按正式上线准备推进。
- 重构文档分工：`AGENTS.md` 作为固定入口，`progress.md` 作为滚动交接，`bugs.md` 保留长期问题，`context.md` 保留项目地图。
- 确认最近一轮仍然有效的产品状态：
  - 课表图片异步解析后端已支持 `processing` 状态与轮询接口。
  - Chat 流式输出、课程周次过滤、旧数据库兼容迁移、OCR 确认卡压缩等关键修复已落地。
- 把“先 PWA 真机验证，再评估 App 封装”的两步走路线写入当前优先级与待办池。

## 当前优先级
1. 主线继续放在 Agent Loop 闭环稳定性。长历史/上下文注入边界、provider/network 可见恢复、连续任务/提醒 evidence、复习计划确认写入、学习上下文 intake、作业/报告 deadline 拆解、多考试排程、计划压缩调整、课程纠错/删除/合并都已收口；课表图片真实 OCR 已确认质量不达标。下一步若继续 Agent Loop，应优先扩课表解析确认后导入、记忆 recall/save 的确认边界，或观察真实自用聊天里是否还有“不接话/确认后空窗”的高频复现。
2. 若后续日常手机自用暴露新问题，先写入 `bugs.md` 并只挑最高价值问题进入修复，不重开移动端大改。
3. Android 封装暂不启动；当前结论仍是继续 PWA，TWA 只作为以后有分发诉求时的候选。

## 下一 Session 第一件事
- 如果继续 Agent Loop：短中期任务拆解已经覆盖单考试、多考试、作业/报告、计划压缩调整；课程维护已经覆盖改名、删除和合并。下一步从课表解析确认后导入、记忆 recall/save 的确认边界中选一个继续补 live E2E；也可以先让用户用真实话术手测几轮，确认还有没有“不接话/确认后空窗”的高频问题。
- Plan 8 手机验收已由用户补充确认 OK；除非出现新问题，不再从移动端收尾继续。

## 当前阻塞 / 风险
- 用户真正指定的两张 `微信图片_20260419104519.jpg` / `微信图片_20260419104523.jpg` 是课表图；真实 OCR 测试会外发图片到 vision provider，需要用户明确授权后再跑。无授权时只做本地多图上传/合并探针、负向回归和 prompt/状态链路保护。
- 任务拆解已证明“能先收集关键上下文，再生成候选计划，并在确认后写入任务”，覆盖单考试、多考试、作业/报告和计划压缩调整。对外表述仍应保守：这是短中期日程任务拆解闭环，不代表长期目标拆解、完整学习诊断、课程纠错/删除、记忆和课表导入确认场景都已同等覆盖。
- Agent Loop live E2E 依赖真实 provider；普通沙箱可能报 `openai.APIConnectionError` / `WinError 5`，应按环境坑处理。判断代码闭环是否失败时优先看沙箱外运行结果、DB invariant、agent_logs 和 evidence JSON。
- 旧 session 的完整交接历史还没有单独归档成 `handoff` 文档；当前依赖本文件做滚动交接、依赖 `bugs.md` 保存长期问题。

## 真机自用待办池
- 自用问题记录机制：后续日常使用中遇到的问题持续写入 `bugs.md`，并区分致命、高摩擦、低优先级。
- App 封装预研：当前结论是继续 PWA，PWA 稳定后再考虑 TWA；暂不引入 Capacitor。

## 最近验证基线
- 最近一次 Plan 15 课程维护验证：`C:\Users\Chen\anaconda3\python.exe -m pytest -q tests/test_agent_loop.py::test_course_merge_shortcut_collapses_duplicate_courses_without_llm tests/test_agent_loop.py::test_course_merge_shortcut_preserves_distinct_week_patterns tests/test_agent_loop.py::test_course_rename_shortcut_updates_existing_course_without_llm tests/test_agent_loop.py::test_course_delete_shortcut_deletes_unique_course_after_confirmation tests/test_agent_loop.py::test_course_delete_shortcut_does_not_delete_ambiguous_same_name_courses tests/test_agent_loop.py::test_course_maintenance_shortcut_does_not_write_when_target_missing tests/test_tool_executor.py::test_update_course_changes_name` 为 `7 passed, 1 warning`；Agent/tool 相关回归 `C:\Users\Chen\anaconda3\python.exe -m pytest -q tests/test_agent_loop.py tests/test_agent_loop_task_creation.py tests/test_tool_preflight.py tests/test_tool_executor.py tests/test_tools_schema.py` 为 `66 passed, 1 warning`；`npm.cmd run typecheck`（在 `student-planner/frontend`）PASS；沙箱外 `npm.cmd run e2e:agent-loop -- --grep "renames an imported course" --reporter=list --global-timeout=420000` 为 `1 passed (13.8s)`。warning 为既有 `.pytest_cache` 权限。
- 最近一次 Plan 13 学习计划拆解质量验证：`C:\Users\Chen\anaconda3\python.exe -m pytest -q tests/test_study_planner.py tests/test_agent_loop_task_creation.py tests/test_tool_preflight.py tests/test_tool_executor.py tests/test_tools_schema.py` 为 `40 passed, 1 warning`；`npm.cmd run typecheck`（在 `student-planner/frontend`）PASS；`npm.cmd run e2e:agent-loop -- --grep "study plan" --reporter=list --global-timeout=420000`（在 `student-planner/frontend`，沙箱外权限）为 `2 passed (1.2m)`。warning 为既有 `.pytest_cache` 权限。
- 最近一次学习计划拆解修复验证：`C:\Users\Chen\anaconda3\python.exe -m pytest -q tests/test_agent_loop_task_creation.py::test_agent_loop_writes_confirmed_study_plan_tasks` 为 `1 passed, 1 warning`；`C:\Users\Chen\anaconda3\python.exe -m pytest -q tests/test_agent_loop_task_creation.py tests/test_tool_preflight.py tests/test_tool_executor.py tests/test_tools_schema.py` 为 `35 passed, 1 warning`；`npm.cmd run typecheck`（在 `student-planner/frontend`）PASS；`npm.cmd run e2e:agent-loop -- --grep "study plan" --reporter=list --global-timeout=420000`（在 `student-planner/frontend`，沙箱外权限）为 `2 passed (56.0s)`。普通沙箱下同一 live E2E 仍可能因 provider 网络权限报 `openai.APIConnectionError` / `WinError 5`。
- 最近一次课表图片解析负向回归：`C:\Users\Chen\anaconda3\python.exe -m pytest -q tests\test_agent_loop.py::test_schedule_import_shortcut_handles_empty_image_parse_without_review_card tests\test_schedule_ocr.py tests\test_schedule_import_api.py tests\test_schedule_tools.py`，`34 passed, 5 warnings`；warnings 为 openpyxl deprecation 和 `.pytest_cache` 写入权限。
- 最近一次 Plan 12 后端定向回归：`C:\Users\Chen\anaconda3\python.exe -m pytest -q tests\test_chat_ws.py tests\test_agent_loop_task_creation.py tests\test_tool_preflight.py tests\test_tools_schema.py`，`30 passed, 1 warning`；warning 为 `.pytest_cache` 写入权限。
- 最近一次 Plan 12 前端 store 回归：`npm.cmd test -- src/stores/chatStore.test.ts`（在 `student-planner/frontend`，沙箱外权限），`14 passed`；普通沙箱中 Vitest setup 绝对路径可能失败。
- 最近一次 Agent Loop live E2E：`npm.cmd run e2e:agent-loop -- --reporter=list --global-timeout=600000`（在 `student-planner/frontend`，沙箱外权限），`4 passed (3.0m)`。
- 最近一次 Agent Loop E2E 范围检查：`npm.cmd run e2e:agent-loop -- --list`（在 `student-planner/frontend`），列出 4 条 live 用例。
- 最近一次前端类型检查：`npm.cmd run typecheck`（在 `student-planner/frontend`），PASS。
- 最近一次 Plan 11 上下文稳定性定向回归：`C:\Users\Chen\anaconda3\python.exe -m pytest -q tests\test_context.py tests\test_context_loading.py tests\test_conversation_compression.py tests\test_loop_compression.py tests\test_agent_loop.py`，`24 passed, 1 warning`；warning 为 `.pytest_cache` 写入权限。
- 最近一次 Plan 11 Agent task/preflight 保护回归：`C:\Users\Chen\anaconda3\python.exe -m pytest -q tests\test_agent_loop_task_creation.py tests\test_tool_preflight.py`，`20 passed, 1 warning`；warning 为 `.pytest_cache` 写入权限。
- 最近一次 Agent Loop live E2E：`npm.cmd run e2e:agent-loop -- --reporter=list --global-timeout=420000`（在 `student-planner/frontend`，沙箱外权限），`3 passed (1.5m)`。
- 最近一次 Agent Loop 定向后端回归：`C:\Users\Chen\anaconda3\python.exe -m pytest -q tests/test_agent_loop_task_creation.py tests/test_agent_loop.py tests/test_tool_preflight.py`，`33 passed, 1 warning`；warning 为 `.pytest_cache` 写入权限。
- 最近一次默认前端 E2E 范围检查：`npm.cmd run e2e -- --list`（在 `student-planner/frontend`），只列出 `app.spec.ts` 1 条默认用例。
- 最近一次全量后端回归：`C:\Users\Chen\anaconda3\python.exe -m pytest -q`，`229 passed, 51 warnings`。
- 最近一次全量前端回归：`npm.cmd test`（在 `student-planner/frontend`），`79 passed, 2 skipped`。
- 最近一次前端构建：`npm.cmd run build`（在 `student-planner/frontend`），`PASS`。
- 最近一次 production preview 烟测：`npm.cmd run preview -- --host 127.0.0.1 --port 4173` 后验证 `manifest.webmanifest=200`、`/chat=200`。
- 最近一次 Plan 8 前端重点回归：`npm.cmd test -- src/stores/chatStore.test.ts src/pages/CoursesPage.test.tsx src/pages/ChatPage.test.tsx src/pages/NotificationsPage.test.tsx`，`54 passed, 2 skipped`。
- 最近一次 Task 2 定向前端回归：`npm --prefix student-planner/frontend test -- src/createClientId.test.ts src/stores/chatStore.test.ts src/pages/ChatPage.test.tsx`，`50 passed`。
- 最近一次 Task 2 定向后端回归：`py -3.12 -m pytest tests/test_chat_ws.py -v`，`4 passed`。
- 最近一轮 Chat 页定向回归：`npm --prefix student-planner/frontend test -- src/pages/ChatPage.test.tsx`，`38 passed, 2 skipped`。
- 最近一轮线上课表文件 smoke：通过 `https://browsing-gibson-cons-aggregate.trycloudflare.com` 注册临时账号、上传混合周次表格、补节次时间和学期信息、确认导入，最终 `/api/courses/` 返回的 `week_pattern` / `week_text` 与预期一致。
- 最近一轮线上推送链 smoke：部署机已生成非空 VAPID 键并重启后端；从服务器本机新注册临时账号后，`GET /api/push/vapid-key` 返回非空 `public_key`，`POST /api/push/subscribe` 返回 `{"status":"subscribed"}`，随后已清理临时假订阅，当前生产数据库再次回到 `users_with_push_subscription=0`。
## 2026-05-02 Agent 任务能力修复
- 已确认并修复一个真实线上能力错配：后端 HTTP 层一直支持 `POST /api/tasks/`，但 agent 工具层之前只有 `list_tasks / update_task / complete_task / set_reminder`，没有 `create_task`，导致真机对话里先口头承诺“我来帮你创建任务”，真正执行时却退化成 `update_task(task_id="new") -> Task not found`。
- 本地已补齐 `create_task` tool definition、tool executor 冲突校验、`update_task("new")` 明确错误提示，以及系统提示里的任务/提醒硬规则；定向回归 `32 passed`，其中包含新的 agent-loop 回归：`17.00提醒我去做饭` 现在能走通“确认 -> create_task -> set_reminder”。
- 服务器 `101.33.229.161` 已同步新的 `app/agent/tools.py`、`app/agent/tool_executor.py`、`app/agent/prompt.py` 并重启 `student-planner-backend.service`；远端已确认 `create_task` 同时存在于 `TOOL_DEFINITIONS` 和 `TOOL_HANDLERS`。
- 下一步需要用户在手机上复测两条真实路径：
  - `17点提醒我去做饭`
  - `下周四要考试，大学英语3，帮我做个复习计划吧`，并在确认后观察是否真正写入日程

## 2026-05-03 Reminder 路由调度修复
- 已定位昨天 `2026-05-02 19:20` 真机“创建了提醒但完全不弹”的直接根因：`/api/reminders/` 路由此前只写入 `reminders` 表，没有调用 `schedule_reminder_job`，所以提醒会永远停留在数据库 `pending` 状态，后台也没有任何 `fire_reminder` 执行日志。
- 本地已给 [reminders.py](/D:/student_time_plan/student-planner/app/routers/reminders.py) 补上“创建后立即 schedule / 删除时 cancel”逻辑，并补齐对应回归；当前任务链 + 提醒调度链定向回归共 `40 passed`。
- 服务器 `101.33.229.161` 已同步新的 `app/routers/reminders.py` 并重启 `student-planner-backend.service`；远端确认 `schedule_reminder_job` / `cancel_reminder_job` 已在路由中生效。
- 那条已经错过的旧提醒 `62b100df-611f-4077-9a39-ec5288dbf1fe` 为避免服务重启后补发迟到通知，已从 `pending` 改为 `failed`。后续请再创建一个未来几分钟的新提醒做真机复测。

## 2026-05-06 Task + Reminder 一体化修复
- 已确认当前真机问题不只是“推送不发”，而是“任务”和“提醒”仍然是松散分开的两套流程：日历页此前只调用 `/api/tasks/` 创建任务，不会同步创建 reminder；因此用户能看到 `11:01-11:31` 的任务，但数据库里实际挂在这条任务上的提醒却可能是完全不同的时间（例如 `22:55`），导致用户误以为“11:01 的提醒没有发”。
- 已在后端 [tasks.py](/D:/student_time_plan/student-planner/app/routers/tasks.py) 和 [task.py](/D:/student_time_plan/student-planner/app/schemas/task.py) 中新增 `reminder_advance_minutes` 支持：创建任务时可选同步创建 task reminder；更新任务时间时会同步重算已有 task reminder；删除任务时会同步取消并删除关联 reminder。
- 已在前端 [CalendarPage.tsx](/D:/student_time_plan/student-planner/frontend/src/pages/CalendarPage.tsx) 中把“添加任务”弹层扩成可选提醒模式，支持“不提醒 / 准点提醒 / 提前15分钟 / 提前30分钟 / 提前1小时”，并透传到 `/api/tasks/`。
- 本地定向验证已通过：
  - 后端 `test_tasks.py + test_reminders.py + reminder scheduler` 共 `18 passed`
  - 前端 `CalendarPage.test.tsx` `8 passed`
  - 前端 build `PASS`
- 服务器 `101.33.229.161` 已同步新的 `app/routers/tasks.py`、`app/schemas/task.py` 和前端 build 产物，并重启 `student-planner-backend.service`；远端已确认 `TaskCreate` 现在包含 `reminder_advance_minutes`，且 `tasks` 路由中存在 reminder 同步逻辑。
- 仍待最终真机确认的一点：远端“临时账号 -> 直接调 `/api/tasks/` -> 检查 `/api/reminders/`”的本机 smoke 在 SSH 下超时，因此这轮我不宣称线上已百分百实测成功；下一步应直接在手机上新建一个 5 分钟后的任务，并在创建时明确选中“准点提醒”或“提前15分钟”进行真机验证。
## 2026-05-09 Scheduler 自启动兜底修复
- 已继续收窄真机不弹提醒的根因：`2026-05-09 11:20` 的 `去做饭` 任务与对应 reminder 都已正确落库，但 reminder 一直停在 `pending`，而 `journalctl` 在 `11:20` 时间窗没有任何 `fire_reminder` 执行痕迹，说明问题已经不在前端传参或写库，而在调度执行层。
- 已在 [reminder_scheduler.py](/D:/student_time_plan/student-planner/app/services/reminder_scheduler.py) 中增加兜底：`schedule_reminder_job()` 现在会在 scheduler 未运行时先 `start()`，并把已经到点/刚过点的 `fire_time` 钳到 `now`，避免整分创建或 scheduler 停止时 job 被静默丢失。
- 本地 scheduler 相关回归 `8 passed`；服务器 `101.33.229.161` 已同步新的 `app/services/reminder_scheduler.py` 并重启后端。
- 为避免服务重启后补发一串过期提醒，这次同步前已把数据库中 `6` 条“已经过期但仍是 pending”的旧 reminder 统一改为 `failed`；当前库里不再残留过期 pending reminder。
- 下一步仍需用户在手机上新建一个 5 分钟后的任务提醒做真机验证；如果再失败，要继续查的是“生产进程里 APScheduler 运行态与 job 注入时机”，而不是权限/VAPID。
## 2026-05-09 推送链最终定位与下一步建议
- 本轮已经把应用层可见的问题基本排干净：task/reminder 一体化创建、`/api/reminders/` 写库后立即 schedule、scheduler 未启动时自启动、到点 pending reminder 的定时 sweep、`send_push()` 的超时与广义异常兜底都已补上，本地相关定向回归通过。
- 线上真实定位结果已经收敛到“服务器出网到 Google/FCM 不通”：
  - 服务器直连 `fcm.googleapis.com:443` 超时；
  - 服务器上的 `gost.service` 虽然在跑，监听 `:18080`，但走该代理访问 `fcm.googleapis.com` / `www.google.com` 都失败（HTTP CONNECT 返回 `503`，SOCKS5 返回连接完成失败）；
  - 直接用用户真实 push subscription 在服务器上调用 `send_push()`，得到的明确错误是 `Failed to establish a new connection: [Errno 101] Network is unreachable` 指向 `fcm.googleapis.com`。
- 因此当前阻塞点已经不是 Student Planner 代码逻辑，而是部署环境网络能力：这台大陆测试机既不能直连 Google/FCM，也不能通过现有 gost 代理打通该链路。
- 下一 session 最现实的两条路：
  1. 优先检查/修复服务器上的代理链（`gost -> 上游节点 -> Google/FCM`），确认能从服务器访问 `https://fcm.googleapis.com`；
  2. 如果不继续折腾大陆服务器，改走“本地电脑开 VPN + 本地运行后端 + 临时 HTTPS 暴露给手机”的方案做真机推送验证。该方案要求测试期间电脑、后端进程和 VPN 都保持在线。
- 本轮停在“记录并交接”，不再继续改代码；下个 session 请先按上面两条路线二选一，再继续推进真机提醒验证。

## 2026-05-24 失效 gost 代理清理
- 已登录测试服务器 `101.33.229.161` 确认代理现状：`/root/.bashrc` 曾把 `HTTP_PROXY` / `HTTPS_PROXY` / `ALL_PROXY` 指向 `http://127.0.0.1:18080`，但 `gost` 上游 `67.209.185.22:6758` 已拒绝连接，导致所有走本地代理的请求返回 `CONNECT tunnel failed, response 503`。
- 已按“停用并备份”方式清理坏代理：`gost.service` 已不再被 systemd 加载，`:18080` 已不再监听；`/root/.bashrc` 中三条代理变量已注释，备份保留在 `/root/.bashrc.proxybak.*`，旧 unit 备份保留在 `/root/gost.service.bak`。
- 清理后新 SSH 登录环境已无 `http_proxy` / `https_proxy` / `all_proxy` 变量；`127.0.0.1:8000` 后端和 `127.0.0.1:8080` Nginx 仍在运行。直接访问 `https://fcm.googleapis.com` 仍超时，因此删除坏代理只是避免误走坏链路，不代表大陆服务器已经能发 Web Push。

## 2026-05-17 方案 B 本地 VPN 推送验证
- 已按“本地电脑开 VPN + 本地后端 + localtunnel HTTPS 给手机”的方案完成一次真机推送验证。因本机 `127.0.0.1:8000` 已有 EngGo 后端占用，本轮 Student Planner 后端改跑 `0.0.0.0:8001`，前端 production preview 跑 `0.0.0.0:4173`，并通过 `STUDENT_PLANNER_BACKEND_ORIGIN=http://127.0.0.1:8001` 让 Vite proxy 指向正确后端。
- 本地 `.env` 原先 `SP_VAPID_PRIVATE_KEY` / `SP_VAPID_PUBLIC_KEY` 为空，本轮已生成本地测试 VAPID key；后端 `/api/push/status` 对登录用户返回 `vapid_configured=true`。
- localtunnel 第一轮地址为 `https://loud-rice-joke.loca.lt`，输入 tunnel password 后被 Vite preview Host 校验拦截；已在 [vite.config.ts](/D:/student_time_plan/student-planner/frontend/vite.config.ts) 加入 `preview.allowedHosts: ['.loca.lt']`，并把 `server/preview` proxy 统一为可配置的 `backendProxy`。前端 build 已通过。
- 当前可用的本地 HTTPS 入口为 `https://flat-parts-travel.loca.lt/register`；localtunnel password/IP 为 `156.229.160.167`。已从外部验证 `/chat`、`/register` 返回 `200`，`/api/push/status` 返回本地 Student Planner 后端的未登录 `403`。
- 账号 `111` 已完成真实手机 push subscription 写入，数据库里 `users.push_subscription` 非空。直接调用 `send_push()` 返回 `ok=True, status_code=201`，用户手机端确认收到一条测试推送。
- 手机端创建的 `10:48` 任务已同步生成 task reminder；对应 reminder `b8f5092d-cd85-4d5f-b103-d76bdeeb3e94` 在到点后从 `pending` 变为 `sent`。但用户随后明确说明没有看到 `10:48` 这条可见通知，因此本轮只能证明：本地可访问 FCM 的环境下，后端调度会执行并被 FCM accepted；尚未证明 scheduled reminder 能稳定在手机系统通知栏展示。下一步应继续查“FCM accepted 但设备未展示”的差异，而腾讯云环境失败仍应按“服务器到 FCM 出网不通”处理。
- 仍有一个新观察：本轮手机端创建的任务标题入库为 `혼넜레`，通知里也出现“Student...B / 一堆问号”之类乱码。用同一 API 通过标准 UTF-8 创建 `中文编码检查` 能正确入库，说明后端/SQLite/推送通道本身支持中文；后续若复现，应单独查 mobile/localtunnel 页面输入链路或该次测试输入来源的编码问题。
- 用户反馈未看到 `10:48 sent` 后，已继续定位出两个容易混淆的因素：
  - `pywebpush.webpush()` 默认 `ttl=0`，即 FCM 不会在设备暂时不可达时缓存消息；现已在 [push_service.py](/D:/student_time_plan/student-planner/app/services/push_service.py) 显式设置 `ttl=3600`，并补了 `tests/test_push_service.py` 断言。
  - 用普通沙箱/Node detached 启动的本地后端不能访问 `fcm.googleapis.com`，会导致 reminder job 执行后继续保持 `pending` 等待重试；方案 B 必须用沙箱外权限启动本地后端，当前有效后端 PID 为 `66860`，监听 `0.0.0.0:8001`。
- 干净复测结果：通过真实 `/api/tasks/` 创建 `11:15` 的 `TTL clean scheduled test`，对应 reminder `0e294d41-9694-45ad-8490-30cbe9b987a5` 到点后变为 `sent`；用户随后确认手机收到 Chrome 弹窗，测试任务已删除。因此在“沙箱外本地后端 + 可访问 FCM + Web Push TTL=3600”的路径下，scheduled task reminder 已完成后端调度、FCM accepted、手机通知展示闭环。
2026-06-18 LangGraph tool node update (in progress): working only in `D:\tmp\student-planner-langgraph-final` on `codex/langgraph-rag-submission`; start state was `## codex/langgraph-rag-submission...origin/codex/langgraph-rag-submission [ahead 1]`, HEAD `df50ce1a806f1ba9b67293bbf9c856cd5f86c755`, upstream `origin/codex/langgraph-rag-submission`, with existing runtime artifacts `student-planner/data/rag/chroma/chroma.sqlite3`, `logs/`, `student-planner/logs/`, plus an untracked failing regression draft `student-planner/tests/test_langgraph_tool_node.py`. Root cause: LangGraph router shell still delegated all generic tool execution to `run_agent_loop()`, so the tool boundary existed only inside the legacy loop. Implementation: added `GraphToolNodeRuntime` and `run_langgraph_tool_node()` in `app/agent/langgraph_loop.py` as a graph-state node harness for one pending OpenAI-style tool call. The node now owns unknown-tool guard, consecutive `ask_user` guard, max-retry guard, task/schema preflight, reminder-slot argument repair, execution dispatch, `tool_result` events, compressed tool summary persistence, agent-log persistence, tool messages, `tool_history`, `error_count`, `last_tool_result`, and `last_free_slots_result` updates. Write tools do not default to direct `execute_tool()` from the node; tests inject a confirmed executor, preserving the existing confirmation/write-plan gate for production integration. Contracts/docs now include `pending_tool_call` and the node migration status. Focused verification: `.\.venv-native\Scripts\python.exe -m pytest -q tests/test_langgraph_tool_node.py` -> `7 passed`; user-specified native regression -> `116 passed, 1 warning`; `git diff --check` -> passed. WS smoke, final status update, and commit still pending.
2026-06-18 LangGraph tool node final verification: native focused regression `tests/test_langgraph_tool_node.py` passed with `7 passed`; user-specified native backend regression `tests/test_agent_contracts.py tests/test_langgraph_rag_runtime.py tests/test_agent_loop.py tests/test_agent_loop_task_creation.py tests/test_chat_ws.py tests/test_tool_preflight.py tests/test_tools_schema.py` passed with `116 passed, 1 warning` (existing Starlette/TestClient deprecation); `git diff --check` passed. Real `SP_AGENT_RUNTIME=langgraph` WebSocket smoke used independent `agent_tool_node_e2e.db` on `127.0.0.1:8001`: ordinary create task/reminder emitted `rag_retrieve_study_materials -> create_task -> ask_user -> create_task -> done` and DB invariant was `tasks=1`, `reminders=1`; modify reminder emitted `list_tasks -> get_free_slots -> update_task -> ask_user -> update_task -> done` and DB invariant was task `16:00-17:00` plus reminder `advance_minutes=15`; a preflight stress prompt emitted repeated guarded `create_task` attempts with zero persisted tasks, while the deterministic missing-argument path is covered by the graph-node regression and `test_tool_preflight.py`. Backend PID `16544` was stopped after smoke. Runtime artifacts remain uncommitted.
2026-06-18 LangGraph tool node deterministic WS preflight smoke: with `SP_AGENT_RUNTIME=langgraph`, a local OpenAI-compatible stub first returned an incomplete `create_task` call, the backend converted it into the internal schema preflight tool message `Tool create_task is missing required argument(s): scheduled_date, start_time, end_time.`, then the stub returned schema-valid `ask_user`, the user answer supplied `2099-06-01 16:00-17:00`, and the retry wrote exactly one task. WebSocket event sequence stayed `connected -> tool_call(ask_user) -> ask_user(review) -> tool_call(create_task) -> tool_result(create_task) -> text_delta -> text -> done`; DB invariant in `agent_tool_node_stub_smoke6.db` was one pending task `Stub preflight task` at `2099-06-01 16:00-17:00`, zero reminders, and agent log order `ask_user`, `create_task`. Runtime smoke DB remains uncommitted.

2026-06-19 LangGraph trace acceptance finalization: in `D:\tmp\student-planner-langgraph-final`, the route-shell runtime now exposes compatible `graph_nodes` trace evidence on text-only golden paths as well as action/RAG tool-result paths. `no_web` emits `text -> done` with top-level `["route", "no_web"]`; `plain_chat` emits streaming text events with `plain_chat`; RAG hit/insufficient text events also carry `rag_qa` / `rag_insufficient`, while action paths continue to expose `task_tool_node`, `ask_user_pause`, `confirmed_write`, `schedule_parse`, `course_disambiguate`, and `plan_review_write` through tool/result events. The migration contract now states the current architecture precisely: LangGraph route shell plus route-specific trace nodes with delegated helper execution, not a complete graph-owned replacement for every legacy loop step; `delegate_legacy_loop` remains a compatibility fallback only and must not appear in golden trace evidence. Verification: focused `tests/test_langgraph_rag_runtime.py tests/test_langgraph_action_workflows.py` -> `43 passed`; full required native regression `tests/test_agent_contracts.py tests/test_langgraph_rag_runtime.py tests/test_langgraph_tool_node.py tests/test_langgraph_action_workflows.py tests/test_agent_db_write_gate.py tests/test_agent_loop.py tests/test_agent_loop_task_creation.py tests/test_agent_loop_schedule_import_shortcut.py tests/test_chat_ws.py tests/test_tool_preflight.py tests/test_tools_schema.py` -> `140 passed, 2 warnings` (Starlette/TestClient deprecation and one existing coroutine resource warning); real WS smoke `scripts/langgraph_ws_smoke.py --db D:\tmp\student-planner-langgraph-ws-smoke-final-trace.db --stub-port 18135` passed with no-web/plain-chat/RAG/action trace evidence and DB invariants; `git diff --check` passed with only CRLF warnings. Runtime artifacts and RAG vector files remain uncommitted.

2026-06-19 LangGraph ask_user state convergence: start state for this slice was `## codex/langgraph-rag-submission...origin/codex/langgraph-rag-submission [ahead 9]`, HEAD `ccac8c3b527ec43a32b4e6285717369c02d8f219`, with only existing Chroma/log/smoke DB runtime artifacts dirty. Implementation: `record_langgraph_ask_user_pause_state()` now mirrors local `ask_user` pauses into LangGraph state; plain-text ask fallback now creates a synthetic `ask_user` tool call and uses `run_langgraph_tool_node()` instead of direct `execute_tool("ask_user")`; local action shortcuts are wrapped by a graph-state proxy that records `pending_ask` / `submitted_answer` and carries `pending_confirmation` plus the first applicable `db_write_plan` as internal metadata stripped before WebSocket emission. Confirmation semantics and frontend event shapes were preserved. Regression evidence: new `test_langgraph_plain_text_ask_fallback_uses_tool_node_state` proves direct `loop.execute_tool("ask_user")` is not used and resume state records the submitted answer; new `test_langgraph_missing_task_shortcut_records_confirmation_state` proves confirm/cancel missing-task shortcut paths keep graph-state pending/answered/confirmation/write-plan evidence and only confirmed writes persist. Verification: focused action/tool-node regression -> `15 passed`; required bundle `tests/test_langgraph_tool_node.py tests/test_langgraph_action_workflows.py tests/test_agent_loop.py tests/test_chat_ws.py tests/test_tool_preflight.py` -> `55 passed, 1 warning` (existing Starlette/TestClient deprecation); real WS smoke `scripts/langgraph_ws_smoke.py --db D:\tmp\student-planner-langgraph-ask-state-smoke.db --stub-port 18145` passed, including create_task confirmation, cancellation, missing-argument recovery, study/work, schedule import, and course maintenance DB invariants; `git diff --check` passed with only CRLF warnings. Runtime artifacts and RAG vector files remain uncommitted.

2026-06-20 LangGraph schedule_import native workflow: start state for this slice was `## codex/langgraph-rag-submission...origin/codex/langgraph-rag-submission [ahead 10]`, HEAD `ec943f1e70116b58fd54906b010dffe8e3348f84`, with only existing Chroma/log/smoke DB runtime artifacts dirty. Implementation: schedule import now uses `GraphScheduleImportRuntime`, `run_langgraph_schedule_import_workflow()`, and `run_langgraph_schedule_import_step()` in `langgraph_loop.py`; `loop.py` only bridges the generator into the existing WebSocket event stream. The LangGraph schedule workflow owns `schedule_parse` execution for `parse_schedule` and `save_period_times`, `ask_user_pause` state for missing period/semester info and final review, and `confirmed_write` validation/execution for `bulk_import_courses`. The legacy `_run_schedule_import_shortcut()` remains in place but is no longer used by the schedule_import action route. Regression evidence: `tests/test_agent_loop_schedule_import_shortcut.py` now spies graph states across `schedule_parse -> ask_user_pause -> schedule_parse -> ask_user_pause -> schedule_parse -> ask_user_pause -> confirmed_write`, including `pending_ask`, `pending_confirmation`, `db_write_plan`, and `confirmed_result`; `tests/test_langgraph_action_workflows.py` proves the golden schedule path does not delegate to `run_agent_loop`, does not call the old shortcut, and does not execute parse/write through `loop.execute_tool`. Verification: focused `tests/test_agent_loop_schedule_import_shortcut.py tests/test_langgraph_action_workflows.py` -> `11 passed, 1 warning` (existing coroutine warning); required native bundle `tests/test_langgraph_action_workflows.py tests/test_agent_loop_schedule_import_shortcut.py tests/test_agent_loop.py tests/test_chat_ws.py tests/test_tools_schema.py` -> `52 passed, 1 warning` (existing Starlette/TestClient deprecation); real WS smoke `scripts/langgraph_ws_smoke.py --db D:\tmp\student-planner-langgraph-schedule-native-smoke2.db --stub-port 18166` passed. Schedule smoke event sequence stayed `parse_schedule -> parse_schedule -> ask_user -> save_period_times -> save_period_times -> ask_user -> bulk_import_courses -> bulk_import_courses -> text -> done`; graph nodes included `schedule_import`, `schedule_parse`, `ask_user_pause`, and `confirmed_write`; DB courses invariant held with imported courses including `高等数学 08:00-09:40`, `线性代数 08:00-09:40`, `大学英语 10:00-11:40`, `大学物理 14:00-15:40`, `体育 16:00-17:40`, plus course-maintenance smoke results. The current schema has no separate `period_times` table, so period-time invariant is verified via the imported `courses.start_time/end_time` fields. `git diff --check` passed with only CRLF warnings. Runtime artifacts and RAG vector files remain uncommitted.
2026-06-20 追加更新：完成 study/work plan 生成、review ask_user、确认后 create_task 写入的 LangGraph workflow 状态机迁移。本轮继续只在 `D:\tmp\student-planner-langgraph-final` 的 `codex/langgraph-rag-submission` worktree 工作；开始状态记录为 `git status --short` 含既有 Chroma/log/smoke DB 运行产物，HEAD = `256429e9668e2c3d80f5b3fe9ab39fb69c198b55`。实现：`langgraph_loop.py` 新增 `GraphPlanWorkflowRuntime`、`run_langgraph_plan_review_write_step()`、`run_langgraph_plan_review_write_workflow()` 和 `run_langgraph_work_plan_workflow()`；study/work route trace 从 `plan_review_write -> confirmed_write` 扩展为 `plan_generate -> plan_review_write -> confirmed_write`，review pause、`pending_confirmation`、首条与逐条 `DBWritePlan`、confirmation answer、逐条 `create_task`、冲突后 `get_free_slots` 重排和 result/text 输出都进入 `plan_workflow` state。`loop.py` 中模型生成后的 `create_study_plan/create_work_plan` 成功分支不再调用 `_run_confirmed_study_plan_write/_run_confirmed_work_plan_write`，本地 work-plan shortcut 也改为 graph workflow；RAG evidence gate/no-web guard、`get_free_slots`、`create_study_plan/create_work_plan`、available_slots 真实结果回填和确认写库规则保持不变。`LANGGRAPH_MIGRATION_CONTRACTS.md` 已同步新 graph shape；`tests/test_langgraph_action_workflows.py` 增强为 spy `run_langgraph_plan_review_write_step()` 并禁止旧 confirmed write helper / work shortcut 被调用，`tests/test_langgraph_rag_runtime.py` 同步 `plan_generate` trace 期望。验证：`$env:TMP='D:\tmp\pytest-tmp-native'; $env:TEMP='D:\tmp\pytest-tmp-native'; .\.venv-native\Scripts\python.exe -m pytest -q tests/test_langgraph_rag_runtime.py tests/test_langgraph_action_workflows.py tests/test_agent_loop_task_creation.py tests/test_chat_ws.py tests/test_tool_preflight.py tests/test_tools_schema.py` -> `93 passed, 2 warnings in 120.38s`，warnings 仍是既有 Starlette/TestClient deprecation 与 APScheduler coroutine cleanup；真实 WS smoke `.\.venv-native\Scripts\python.exe scripts\langgraph_ws_smoke.py --db D:\tmp\student-planner-langgraph-plan-workflow-smoke2.db --stub-port 18186` 通过，study 序列为 `rag_retrieve_study_materials -> get_free_slots -> create_study_plan -> ask_user -> create_task -> result/text -> done`，work 序列为 `rag_retrieve_study_materials -> get_free_slots -> create_work_plan -> ask_user -> create_task... -> result/text -> done`，两条路径 graph nodes 均包含 `plan_generate/plan_review_write/confirmed_write`，DB invariant 含 1 条 `English 3` 复习任务和 5 条 `ML report` 作业任务；`git diff --check` 通过，仅 CRLF warning。既有/本轮运行产物仍未纳入提交：`student-planner/data/rag/chroma/*`、`logs/`、`student-planner/logs/`、旧 `agent_tool_node_*.db`、`student-planner/data/rag/chroma/rag_vectors.sqlite3`。
