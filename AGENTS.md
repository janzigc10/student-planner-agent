# Student Planner 协作入口

## 项目一句话
AI 驱动的学生时间规划 Agent，核心能力是课前提醒与 AI 任务拆解。

## 新 Session 必读
1. 本文件：协作规则与入口。
2. `progress.md`：滚动交接文档，只看当前阶段、本 session 结果和下一步。
3. `bugs.md`：已知问题、环境坑、延期项。
4. 仅在任务需要时再读 `context.md`、相关 spec、相关 plan、代码与测试。

## 高频硬规则
- 默认使用中文沟通。
- 非简单任务先读相关代码、测试、设计，再开始修改。
- 严格按当前活跃 plan 的 step 顺序执行。
- 每完成一个 step，立即把对应 plan 中的 `- [ ]` 改为 `- [x]`。
- 每完成一个 task，立即更新 `progress.md`。
- 默认最小改动，不做与当前目标无关的重构。
- 遇到设计冲突、需求不清或异常现象，先记录到 `bugs.md` 或显式提出，不自行改设计。
- 测试未通过，不进入下一个 task。

## 当前焦点
当前已完成 Plan 1 至 Plan 8，进入 Agent Loop 稳定性与体验收尾阶段。

当前优先关注：
- Agent Loop 工具执行闭环、确认后落库、失败恢复与 live E2E 证据。
- 复习计划生成后写入任务的确认链路。
- 课表图片真实 OCR 质量与后处理策略。

## 四份文档的职责
- `AGENTS.md`：只放入口和高频硬规则，不写历史流水。
- `context.md`：只放长期稳定的项目背景、结构和文档地图。
- `progress.md`：滚动交接文档，只保留当前 session 交接所需的信息。
- `bugs.md`：只放确认过的问题、环境坑、失败方案和延期项。
